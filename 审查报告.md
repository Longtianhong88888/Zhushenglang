# 今日改动审查报告（2026-08-14）

> 审查对象：自 2026-08-07 提交后工作区的全部未提交改动（约 30 个修改文件 + 40 个新模块/测试 + deploy 脚本，约 21000 行）。
> 方法：4 个并行只读审查 agent 分片审查（模型核心 / 运行时基础设施 / 已跟踪文件 diff / deploy 与测试），其中部署路径矛盾与测试污染两条已由主会话复核属实。
> 测试状态：`python3 -m unittest discover -s tests` → **158 项全绿**。

---

## 总体结论

核心模型信号路径无前视偏差、OOM 教训已遵守（monitor 盘中零全量加载）、删除文件无残留引用、敏感 key 文件已被 .gitignore 覆盖。但发现 **8 个高危问题**，其中 2 个会污染研究结论/真实数据文件，另有约 20 个中等问题。

---

## 一、高危问题（建议优先修复）

### H1. 「跌破 MA60 退出 +85%」的研究结论建立在死规则上
- 位置：[mainrise/bigtrend.py:229-268](mainrise/bigtrend.py#L229-L268)
- 缺陷：`tail_features`（[signals.py:108-122](mainrise/signals.py#L108-L122)）只输出 ma5/10/20，**从未计算 ma60**；`exit_rules` 里 `"ma60" in t.columns` 恒 False → ma60 退出规则从不触发，统计实际是"死拿 150 日窗口末"。
- 影响：AGENTS.md 中"主仓跌破 MA60 才走（大牛信号平均 +85%）"的结论建立在这条假数据上；`honghe_case` 因 exit_j=None 被跳过，案例表永远缺 ma60 行。
- 对比：`candidate_bt.py:51` 与 `portfolio_bt.py:48` 都本地补算了 ma60，唯独 bigtrend 没有。
- 建议：补算 ma60 后重跑 `mainrise bigtrend`，核对结论是否仍成立。

### H2. monitor 提醒限频键是全局的，急跌日卖出提醒会被吞
- 位置：[mainrise/monitor.py:447-454](mainrise/monitor.py#L447-L454)
- 缺陷：止损 / 回落8% / 破 MA10 / 破 MA20 的冷却键不含 code（同函数内 B3/启动信号是按票的）。
- 触发场景：多只持仓同日破位（大盘急跌日），第一只触发后 10 分钟内其余持仓的**卖出提醒全部漏报**。
- 建议：冷却键改为 `stop_{code}` 等按票限频。

### H3. monitor 主循环无兜底，一个渲染异常可杀死盯盘服务
- 位置：[mainrise/monitor.py:1254-1269](mainrise/monitor.py#L1254-L1269)
- 缺陷：主循环只捕获 KeyboardInterrupt，`run_one_cycle` 无异常兜底。
- 已复现崩溃向量：东财兜底路径 `_em_sector_flow` 对停牌板块返回 `chg="-"`（字符串）→ `_chg_span("-")` 比较抛 TypeError。
- 影响：systemd Restart=always 会拉起，但冷却/提醒状态丢失（可能重复提醒）+ 宕机窗口漏警。
- 建议：`run_one_cycle` 整体 try/except 打日志后继续下一轮。

### H4. GUI「财务评估/实时行情」留空 API Key 即不可用
- 位置：[mainrise/gui_pyqt.py:617-624](mainrise/gui_pyqt.py#L617-L624)（`_task_evaluate`/`_task_snapshot`）
- 缺陷：evaluate/snapshot 已迁移到腾讯/东财免费公开接口、帮助文案教用户"可留空"，但按钮仍 `need_key=True`，输入框为空时弹"请先填写 API Key"并直接 return。
- 建议：两个任务改为 `need_key=False`。

### H5. 买点2 研究章节恒为空样本（静默失效）
- 位置：[mainrise/entry_study.py:515-516](mainrise/entry_study.py#L515-L516)
- 缺陷：过滤条件 `label != "回踩低吸"`，而新 `row_status` 只返回 二波加仓/B3打底仓/B3待二波/观察，永不返回"回踩低吸" → 报告输出"共 0 个"，无任何报错。
- 建议：改用新的 row_status 标签或明确下线该章节。

### H6. 测试会污染真实状态文件
- 位置：[tests/test_price_events.py:22-24](tests/test_price_events.py#L22-L24)
- 缺陷：setUp 设置 `paths._ROOT/_MODE` 做路径隔离，但 [mainrise/paths.py](mainrise/paths.py) 中**不存在这两个属性**（已 grep 证实）→ 隔离是死代码，测试把 mock 数据写进真实的 `output/state/price_events.json` 和 `output/reports/产业链涨价追踪_*.md`（output/ 已 gitignore，污染不可见）。
- 建议：在 paths.py 实现真正的可注入根目录，或测试内 mock 输出路径。

### H7. 部署路径三处互相矛盾，默认部署即坏
- 位置：[deploy/setup_server.sh:12](deploy/setup_server.sh#L12)（`APP_DIR=${MAINRISE_APP_DIR:-/root/chaoduan}`）vs [deploy/mainrise-monitor.service:9-11](deploy/mainrise-monitor.service#L9-L11) 与 README（硬编码 `/home/ubuntu/chaoduan`）
- 触发场景：按 setup_server.sh 默认跑 → nginx worker（www-data）无法穿越 0700 的 /root → 全站 403、monitor 服务起不来；反之按 README 放 /home/ubuntu，setup_server.sh 未传参数时 sed 出的 nginx root 指向 /root/chaoduan → 同样 403。
- 建议：统一为 `/home/ubuntu/chaoduan`（与已部署服务器一致）。

### H8. health_check 每周一必然误告警"数据停留在周五"
- 位置：[deploy/health_check.sh:40-44](deploy/health_check.sh#L40-L44)
- 缺陷：`exp=昨天（自然日）`，而 `bigbull_cands.json` 的 `updated`=最后交易日 → 周一 09:05（或长假后首个交易日）固定报 stale。告警疲劳后可能掩盖真实故障。
- 建议：用 trade_dates 判断"今天是否为交易日且数据应更新到昨天"，非交易日跳过。

---

## 二、中等问题

### 模型与研究

- **M1 HOT_MODE="dynamic" 潜伏失效**：[bigbull.py:60](mainrise/bigbull.py#L60) `HOT_RANK_TOP=10` 但 [themeindex.py:175-189](mainrise/themeindex.py#L175-L189) 主题池仅 8 个 → 每天 8 主题全是"热主题"，等价于已被证伪的"放开热主题门槛"（+102%/MDD-80%）。默认 static 不受影响，但一键切换即自毁。
- **M2 CSV 兜底静默换池 + 打包未带名单**：[report.py:14-73](mainrise/report.py#L14-L73) 内置兜底名单仅 36 只且含 12 只已删除股；[build_gui_macos.sh:20-28](build_gui_macos.sh#L20-L28) / [build_gui_windows.bat](build_gui_windows.bat) **未打包 industry_info.csv** → 打包版 GUI 卡点范围从 110 家静默缩水到 36 家。
- **M3 纸面账本与"明日开盘"规则不符**：[tracker.py:168-173](mainrise/tracker.py#L168-L173) 两级模型规则是"明日开盘建仓"，账本却按信号日收盘价当天建仓——隔夜跳空不体现，盈亏/止损基准错。
- **M4 空交易 KeyError**：[portfolio_bt.py:271-272](mainrise/portfolio_bt.py#L271-L272) 零交易返回无列名 DataFrame，后续 `sort_values("entry_date")` 直接 KeyError（已知"先判空再访问列"类坑）。
- **M5 load_theme 独立模式崩溃**：[bigtrend.py:58-59](mainrise/bigtrend.py#L58-L59) 硬编码 `paths.home()/mainrise/resources/industry_info.csv` 相对路径（打包 .app / 非项目根 cwd 时不存在），无 try/except；bigbull/portfolio_bt/candidate_bt/wave/bullcnt 全部受牵连。本机因 `~/.mainrise→项目` 软链被掩盖。
- **M6 二波回测口径偏窄**：[backtest.py:137-158](mainrise/backtest.py#L137-L158) 每个 B3 窗口命中一次即 break，而 signals.py 的 wave2 逐日判定、同一窗口可多次触发 → 回测样本与实盘信号语义不一致；且 `two_stage_run()` 与 `run()` 完全等价，`--two-stage` flag 无实际差异。
- **M7 evaluate 取数列待验证**：[evaluate.py:47-48](mainrise/evaluate.py#L47-L48) 从东财业绩报表端点请求 `XSMLL`（毛利率）/`WEIGHTAVG_ROE`（ROE），这两列可能属"主要指标"端点；若接口不返回则评级系统性偏低。需一次真实调用确认。

### 推送 / 安全

- **M8 push 用服务器本地时间**：[push.py:171,396](mainrise/push.py#L171) `datetime.now()` 而非北京时间（项目不变式是固定 UTC+8）——CN 服务器无影响，属潜伏 bug。
- **M9 `push --close --wecom` 静默忽略 close**：[cli.py:211](mainrise/cli.py#L211) wecom 分支只发 14:50 尾盘消息，无任何警告。
- **M10 `settings.json` 未进 .gitignore**：push 项目模式下读项目根 settings.json，用户按文档写入 Server酱 key/企业微信 webhook 后有被提交泄露的风险。
- **M11 14:50 推送与周五周报无 cron 安装入口**：setup_server.sh 只装 17:30 一条；health_check 09:05 / 14:50 push / 周五 weekly / 周日 night_research 只存在于 AGENTS.md（服务器 crontab 可能有仓库外手工条目，待确认）。
- **M12 live.json 非原子写竞态**：[push.py:161-174](mainrise/push.py#L161-L174) 14:50 恰是盘中轮询窗口，cron 触发的 push 若读到截断 JSON 会被当"非交易日"静默跳过，无重试；`/refresh?page=live` 在 HTTP 线程跑完整 `run_one_cycle` 与主轮询线程并发写同一文件。

### 文案 / 界面一致性

- **M13 时间止损文案四处不一致**：代码 `TIME_STOP=20`，但平仓 reason 写死"5日时间止损"（[tracker.py:265](mainrise/tracker.py#L265)）、报告头（[tracker.py:468](mainrise/tracker.py#L468)）与 [report.py:157](mainrise/report.py#L157) 仍写"5 日"，[excel_report.py:118](mainrise/excel_report.py#L118) 写"10-20 个交易日"。
- **M14 仪表盘防追高阈值未同步**：[dashboard.py:42](mainrise/dashboard.py#L42) `DEFAULT_MAX_10D_GAIN=80.0`，Summary 的 COUNTIFS 用 `<80` → 仪表盘买点计数系统性少计；[help_content.py:71](mainrise/help_content.py#L71) 同样写"≥80%"。
- **M15 README 旧模型描述**：[README.md:89-91](README.md#L89-L91) 仍写买点1/买点2、止损-5%、5 日时间止损，与两级模型（B3/二波、-4%、20 日）冲突。
- **M16 help_content 自相矛盾**：[help_content.py:44-70](mainrise/help_content.py#L44-L70) "模型分析"页写"需要 API Key"（红字），"快速上手"页写"无需 Key、可留空"；"仅本次会话有效"与实际（写入 settings.json 并自动填入）相反。

### 运维脚本

- **M17 chown 仅在流水线全部成功后执行**：[daily_update.sh:46-47](deploy/daily_update.sh#L46-L47) 任一步失败即退出 → 当日 output/ 残留 root 属主文件，monitor 次日无法覆写 → 级联故障。
- **M18 daily_update 无交易日判断**：节假日 evaluate 若东财抖动失败会误告警；长假工作日全量流水线空转。
- **M19 systemd EnvironmentFile 无 `-` 前缀**：[mainrise-monitor.service:10](deploy/mainrise-monitor.service#L10) `.monitor_env` 缺失即起不来，且 setup_server.sh 不创建该文件也不安装该服务。
- **M20 nginx /refresh 无 proxy_read_timeout**：[nginx_web.conf:28-32](deploy/nginx_web.conf#L28-L32) page=live 触发分钟级全市场扫描，默认 60s 超时 → 504。
- **M21 night_research 无超时控制 + 每次成功也发 Server酱消息**：网络卡死会无限阻塞周日 cron；"完成告警"占用免费 5 条/天配额。
- **M22 monitor 在 trade_dates 缺失/过期时静默全天不轮询**：[monitor.py:58-90](mainrise/monitor.py#L58-L90) 文件不存在返回空 set → 一律判"节假日休市"，只打日志无告警。
- **M23 market_state 上证接口失败时每 3 秒重试**：[market_state.py:222-238](mainrise/market_state.py#L222-L238) 失败不更新缓存时间戳 → 接口宕机期间 20 次/分钟持续打腾讯接口。
- **M24 price_events 风控判定过粗**：[price_events.py:104-117](mainrise/price_events.py#L104-L117) 结果缺 `cmsArticleWebOld` 键一律当风控 → 合法空结果也 sleep(30)；反向：网络异常走 break 返回 `[]`，不进腾讯回退也不重试，该产品静默记 0 条。

---

## 三、低危 / 建议（分组速览）

**一致性/文档**
- [weekly.py:67-68](mainrise/weekly.py#L67-L68) 净值不足 6 行时 `iloc[-6]` IndexError（已复现，新账户周五跑周报即崩）。
- [portfolio_bt.py:162-165](mainrise/portfolio_bt.py#L162-L165) 单笔收益只扣一次费用（往返应 ~-0.4% 记 -0.2%；净值曲线正确，胜率/PF 高估 0.2pp/笔）；[:7](mainrise/portfolio_bt.py#L7) docstring 写"退出跌破 MA60"实为 MA20；[backtest.py:160](mainrise/backtest.py#L160) 打印"2022 起"实际含 2021 样本。
- 多个模块文件名日期用本地时区（push/weekly/industry_trend/price_events/cycle_check/launch/strategy/theme_* 的 `datetime.now()`），CN 服务器无影响。
- [gui_pyqt.py:897](mainrise/gui_pyqt.py#L897) "关于"对话框仍描述旧 T0 模型；[cli.py:97-107](mainrise/cli.py#L97-L107) `--no-dashboard` 实际连 review 一起跳过但文案未提。
- [dashboard.py:286-289](mainrise/dashboard.py#L286-L289) Summary"破位/空头"计数恒为 0（新 row_status 不再产出）；F 列把"B3待二波"计入买点提示，与 tracker 买点定义不一致。
- [tracker.py:308-311](mainrise/tracker.py#L308-L311) 两级模型扫描用 110 只全名单（含不可交易的 301/688），与大牛模型 97 只口径不一致（框架由用户维护，若有意可忽略）。
- [tracker.py:401-431](mainrise/tracker.py#L401-L431) `ck` 定义在上一 try 块内，launch 导入失败时第六节整体静默缺失。

**HTML 注入（当前无可利用性，接外部文本前需补）**
- [bigbull.py:305-419](mainrise/bigbull.py#L305-L419) 门户 4 张状态卡（周期/涨价追踪/景气度/热主题）注入未转义（主表格已 `_esc`）；[stockpage.py:919-946](mainrise/stockpage.py#L919-L946) 股票名未转义插入 `<title>/<h1>`；[cycle_state.py:231-233](mainrise/cycle_state.py#L231-L233) error 未转义。

**健壮性**
- [bigbull.py:305-311](mainrise/bigbull.py#L305-L311) 周期卡 `{t["xs60"]:+.0f}` 遇 None → 整卡被裸 except 静默丢弃；[bigbull.py:124](mainrise/bigbull.py#L124) `model_candidates` 零信号时空序列 max ValueError（run() 都传了 last_date，边缘）；[bigbull.py:596-617](mainrise/bigbull.py#L596-L617) dynamic 模式行业指数缓存滞后 → 最新交易日信号全被过滤。
- [themeindex.py:113](mainrise/themeindex.py#L113) 接口脏条目直接 KeyError；[survey_check.py:65](mainrise/survey_check.py#L65) 响应缺 pages 字段时静默只拉第 1 页；[theme_walkforward.py:135-136](mainrise/theme_walkforward.py#L135-L136) 脆弱 f-string 拼接 + 净值无 2025 行时 IndexError；[price_watch.py:203-217](mainrise/price_watch.py#L203-L217) f-string 对 None 格式化崩溃。
- [monitor.py:209](mainrise/monitor.py#L209) `report_date` 为空串时 `sig_date != report_date` 恒真 → 任意历史 B3/二波信号都进监控并提示"明日开盘打底仓"。
- [cli.py:208-215](mainrise/cli.py#L208-L215) cmd_push/cmd_alert 恒 exit 0，`set -e` 失败告警 trap 永不因推送失败触发。

**性能**
- [bullcnt.py:71](mainrise/bullcnt.py#L71) 每个信号行重读一次 industry_info.csv（数千次磁盘 IO）；[theme_scope.py:95](mainrise/theme_scope.py#L95) 表格循环内每行 `load_theme()`（5 次全量加载）；[wave.py:123-126](mainrise/wave.py#L123-L126) / [bigtrend.py:189-230](mainrise/bigtrend.py#L189-L230) 每信号重算全史 tail_features + 全表筛选（随信号数超线性变慢）。

**测试卫生**
- [test_push.py:78-82](tests/test_push.py#L78-L82) finally 里 `del os.environ[...]` 而非恢复原值；[test_stockpage.py:83-88](tests/test_stockpage.py#L83-L88) 缓存恢复不在 finally；[test_market_state.py:134-139](tests/test_market_state.py#L134-L139) 全局缓存变异无回滚；[test_review.py:119-122](tests/test_review.py#L119-L122) 残留指向已删临时目录的缓存；[test_portfolio_bt.py:76-106](tests/test_portfolio_bt.py#L76-L106) `if len(tr):` 包裹断言可静默空过；[test_bigbull.py:19-24](tests/test_bigbull.py#L19-L24) 常量快照测试（调参即红，需知晓其性质）。
- [test_tracker.py](tests/test_tracker.py) 原平仓逻辑测试被整体删除，新自动建仓（B3 开仓/二波加权成本/板块退潮/并行上限）零覆盖。

**部署杂项**
- [package_data.sh:21-25](deploy/package_data.sh#L21-L25) 全新 clone 无 output/state 时 tar 报错退出；[health_check.sh:25-51](deploy/health_check.sh#L25-L51) JSON 损坏时 `"bad-file" < exp` 抛 TypeError → 静默通过（fail-open）；[health_check.sh:57-63](deploy/health_check.sh#L57-L63) free 命令缺失时误报"可用内存 0MB"；日志无轮转上限；告警与流水线共用同一 venv（venv 损坏时告警也发不出）。

---

## 四、已验证无问题（放心项）

- **158 项测试全绿**（17s）；主模型（bigbull/portfolio_bt）信号→收盘买入→收盘 MA20 卖出时序**无前视偏差**（T0 分量均 shift(1)、90 日计数、hi60 只用当日及以前数据；B3 "S+1 确认→S+2 开盘买"、二波 "T 收盘判定→T+1 开盘买"顺序正确）。
- 规则一致性：HOT_MODE 默认 "static"、90日T0≥3、评分≥2、MA20 退出、杀跌区停开、0.2% 费用、1/3 仓×3 只、97 只范围（CSV 实测 110-3-10=97，无重复代码）均与 AGENTS.md 一致。
- monitor 盘中路径零全量加载（OOM 教训已遵守）；`_md_to_html` 所有分支先 `html.escape`，防注入属实。
- 删除的旧脚本（build_macos.sh 等）全仓无残留引用；CI workflow 正常；构建脚本引用的资源文件（klinecharts.min.js/模板/图标）全部存在。
- industry_info.csv 结构干净（110 行、无重复、无垃圾字符）；`.serverchan_key`/`.wecom_webhook`/`.hithink_key` 均已 ignore；push 消息体不含敏感信息；`--dry-run` 各路径只打印不发送。
- 测试总体质量良好：绝大多数真断言、失败路径有覆盖、网络依赖全部 mock 离线可跑。

---

## 五、建议修复顺序

1. **H1** 补算 ma60 重跑 bigtrend → 核对"+85%"结论（研究结论可信度）
2. **H2/H3** monitor 冷却键按票 + run_one_cycle 兜底（卖出提醒漏报 + 服务被杀）
3. **H4** GUI 按钮 need_key=False（新用户可用性）
4. **H6** 测试路径隔离实现（真实数据被污染）
5. **H5** entry-study 买点2 章节（静默失效）
6. **H7/H8** 部署脚本路径统一 + 巡检交易日判断（服务器侧）
7. 中等问题按需分批：M2（打包名单）、M3（账本口径）、M9-M12（推送链路）、M13-M16（文案一致性）
