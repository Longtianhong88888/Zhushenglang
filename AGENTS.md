# 项目记忆（自动加载）

## 项目定位

A 股主升浪信号跟踪模型，已封装为软件：

- `mainrise/` Python 包：信号扫描 / 财务评估 / 综合评分 / 每日跟踪 / 实时行情 / Excel 报告
- `mainrise/gui_pyqt.py`：PyQt5 图形界面（**不要用 Tk**，macOS 渲染空白，已废弃）
- `dist/主升浪跟踪.app`：macOS 打包产物（onedir，含内置行情数据）
- GitHub `Longtianhong88888/Zhushenglang`：代码仓库 + Actions 自动打包 Windows 轻量版

## 核心模型规则

### 大牛模型（固化版，2026-08-14 用户确认）

> 回测 2021-08 ~ 2026-08-12：**+777% / 年化 +50% / 最大回撤 -34% / PF 2.96 /
> 胜率 44% / 108 笔全平仓**（2025 +146%、2026 +102%）。命令 `mainrise bigbull`。

- **追踪范围**：行业卡点企业 **97 只**（110 - 3 只 301 创业板注册制 - 10 只 688 科创板；
  **300 创业板可交易**；名单 `mainrise/resources/industry_info.csv`）
- **入场（信号日收盘买入，需同时满足）**：
  - 硬规则：**热主题**（AI硬件/半导体/存储）且 **90 日内累计 T0 信号 ≥3**
  - **大牛评分 ≥2**：热主题 +1 ｜ 90日T0≥3 +1 ｜ 创60日新高且10日<30% +1 ｜
    链长≥4（相邻信号间隔≤20日）+1（硬规则下评分天然 ≥2，门槛保证质量下限）
  - **热主题动态化研究（2026-08-14，四方案全部负优化）**：日线动量 Top3 +281%、
    日线趋势过滤 +293~477%、月线级排名 Top10（同花顺全A行业指数，2022+ 最优
    +563%）均差于固定三主题（2022+ +679%）；**放开热主题硬门槛最差**
    （+102%/MDD-80%）——固定 AI硬件/半导体/存储 恰锁 2022-2026 科技超级周期，
    是模型最大 alpha 来源。**默认 HOT_MODE="static"**；动态实现
    （`mainrise themeindex`，同花顺 API）保留于 `bigbull.HOT_MODE="dynamic"`
    一键切换（HOT_RANK_TOP=10 / HOT_LOOKBACK_DAYS=60）。
  - **热主题范围研究（2026-08-14，`mainrise theme-scope`）**：把三主题扩大到
    "科技"（+机器人/商业航天/自动驾驶）**确认负作用**：T3（+777%/回撤-34%/
    PF2.96）→ T6 科技（+647%/-36%/2.76）→ T6+有色（+533%/-53%）→ T8 全八
    （+418%/-52%）→ 全放开（+102%/-80%）单调恶化；新增 3 主题 9 只仅 5 笔交易
    均收 -2.5%（劣于整体 +8.7%）——三主题恰锁 2022-2026 最强子集，热主题门槛
    是质量过滤器，越窄越强（固定周期内），保持现规则不变。
  - **硬规则主题 walk-forward 检验（2026-08-14，`mainrise theme-wf`）**：
    "2025 年初定义什么主题"——用训练期（2021-2024）各主题作为唯一热主题的
    模型收益排名选 Top3（交易数≥3 门槛）：AI硬件+52%/半导体+52% 断层领先
    → 选 AI硬件/半导体/存储 = 事后最优组合，2025 年 +390%（选错第三主题
    商业航天 +234%、周期卡当时主线缺 AI硬件仅 +119%）。**结论：主题选择可
    事前执行（每年初按过去 3-4 年主题 alpha 排名），AI硬件/半导体有持续性
    非后视镜；'当下动量'（周期卡）不如'累计 alpha'（训练期收益）适合选硬
    规则主题**；单主题 2025 最强 AI硬件 +176%（有色 +125% 第二但训练期
    -23% 最差——主题会轮动，靠信号门槛兜底）。
  - **多窗口训练检验（2026-08-14，同 `theme-wf`）**：训练窗口 半年/一年/
    一年半/四年 全部稳定选出 AI硬件/半导体/存储（2025 +390%=事后最优）；
    **两年窗口例外**——2023-2024 存储 -10% 排末位，选创新药 → 2025 仅
    +207%（-183pp 但前两主题保底）。**结论：最短可靠窗口=半年（AI硬件/
    半导体任意窗口断层前二），一年更稳；两年有"覆盖主题低谷"的特殊风险**。
    建议每年初用过去 6-12 个月主题收益排名（交易数≥3 门槛）选 Top3。
  - **主题训练频率研究（2026-08-14，`mainrise theme-freq`）**：滚动 walk-forward
    每季度/半年/年重选主题（250 日窗口收益排名）**全部大幅劣于固定 T3**：固定
    +777% vs 每季度 +226%/每半年 +290%/每年 +237%——动态重选被短期反转带偏
    （2022 初选有色→2022 崩、2025 初选机器人→2025 弱，而固定 T3 恰好覆盖
    2021-2026 每一年最强组合）。**结论：不重选，固定 T3；主题选择=长周期累计
    alpha 一次性锁定，任何频率的滚动重选都是追热点负优化**（与热主题动态化
    同构）。
  - **每年 1 月选主题检验（2026-08-14，`mainrise theme-jan`）**：决策日=每年
    1 月首个交易日、训练窗口=前 250 交易日（一年）：**+344% vs 固定 T3 +777%**
    （-433pp）。轨迹：2024/2025 初选对（AI硬件/半导体/存储 → 与固定持平
    +30%/+146%），但 2022 选有色（2021 强→2022 崩）、2023 门槛失效全选、
    2026 选有色（→+57%）——**5 年里只有 2 年选对，年度主题选择近半概率踩中
    反转**；固定 T3 的优势=AI硬件/半导体/存储 六年中每年最强或次强（长周期
    持续），无需每年赌。符合"重选跑输固定"的预期。
  - **产业趋势研究（2026-08-14，`mainrise industry-trend`）**：用户指出"固定
    T3 终会失效，需研究产业趋势做决策"——拉 8 主题龙头 2021-2026 季度财报
    （东财 RPT_LICO_FN_CPD，无前视：报告期+披露延迟 Q1→05-15/中报→08-31/
    Q3→10-31/年报→次年04-30），净利同比中位数=主题景气度。**关键发现**：
    ① 2025-01 时点 AI硬件+156%/半导体+73%/存储+68% 高位 → 产业信号确认科技
    延续（支持固定 T3）；② 2022-01 时点有色 +220% 但已连续 3 季下滑
    （+288→+275→+220）→ 增速**方向**可预警 2022 有色陷阱；③ **2026-01 有色
    +64% 仍高（盈利好但股价弱——涨价周期估值见顶，产业信号失效场景）**。
    结论：产业信号做"确认/否决"（增速高位确认持有、连续下滑否决换入），
    价格信号做交易，两者并用；财务绝对值不万能（2026 有色）。
  - **产业趋势 v2：单季口径 + 业绩预告（2026-08-14，同 `industry-trend`）**：
    用户要求"用季报、缩短时间跨度"——① **单季净利同比**（累计归母净利
    PARENT_NETPROFIT 同财年差分，比累计更敏感：2025-01 半导体单季 +139%
    vs 累计 +73%、2022 有色单季 +172% vs 累计 +220%，回落趋势更明显）；
    ② **业绩预告填补**（RPT_PUBLIC_OP_NEWPREDICT，INCREASE_JZ 中值，比
    财报早 1-3 个月：年报预告 1 月底/中报预告 7 月）——无财报期（1-4 月、
    7-8 月）用预告标注 *，信息跨度从 3-5 个月缩短到 1-2 个月。当前（2026-08）：
    存储中报预告 +1687*（涨价周期爆发）、半导体 +591*、AI硬件 +73*。
  - **产业景气度卡上线（2026-08-14）**：`mainrise industry-trend` 现生成
    industry_trend.json + 门户"产业景气度"卡（bigbull 注入，与周期卡并列）——
    各主题最新单季净利同比中位数 + 环比方向（↑↓→）+ 状态标签（高景气/温和/
    负增长）+ ⭐标记固定热主题；daily_update 顺序 cycle-state → industry-trend
    → bigbull。当前（2026-08-14）：AI硬件 +90%*/半导体 +83%*/存储 +1099%*
    （高景气，支持固定 T3）、有色 +68%*（涨价兑现）、创新药 +7%、自动驾驶 -1%。
  - **供需涨价监控上线（2026-08-14，`mainrise price-watch`）**：新浪期货历史
    日线（免费，沪铜 2005 起/碳酸锂 2023 起）——铜/铝/锌/锡/镍/碳酸锂（有色）
    + 金/银，20/60/120 日涨幅 + 60 日历史分位 + 状态（**见顶回落⚠**=120日内
    60日涨幅峰值>15%且当前回落>10pp / 加速涨价 / 高位 / 温和 / 下跌）。门户
    "供需涨价监控"卡（与周期卡/景气度卡并列）。**验证：期货比财报早 3-6 个月
    预警有色陷阱**（2022 锡 2021-09 +23%→2021-12 +6%→2022-06 -42%；2026 碳酸锂
    2025-12 +67%→2026-08 -13%）；当前（2026-08）：铜/锡/镍/碳酸锂全部见顶回落、
    铝下跌——**有色涨价动能衰竭，主题层否决参考**（勿因涨价追入见顶品种）。
  - **产业链涨价追踪（2026-08-14，`mainrise price-events`）**：东财资讯
    搜索（search-api-web，翻3页）——**按产品名+涨价搜索**（用户 2026-08-14
    指示：按企业名搜 0 命中——新闻标题很少同含公司名+涨价词；产品词才有
    命中率）。**ITEMS=49 个卡点产品关键词**（用户点名：光纤/覆铜板/MLCC/
    HBM/DDR4；补充 DRAM/DDR5/NAND/闪存/存储芯片/SSD→存储，光模块/CPO/铜箔/
    电子布/PCB/液冷/散热/高速铜缆/玻璃基板/金刚石→AI硬件，晶圆/硅片/光刻胶/
    靶材/半导体设备/封测/MCU/模拟芯片/功率半导体/IGBT/碳化硅/被动元件→半导体，
    稀土/钨/锂/钴→有色，减速器/伺服/丝杠→机器人，卫星/火箭/相控阵→商业航天，
    激光雷达/域控→自动驾驶，创新药/原料药/CXO→创新药）。标题过滤：含产品词+
    涨价意图词（涨价/提价/上调/上涨/飙升/暴涨/涨幅/上行/紧缺/短缺/缺货/供不应求/
    景气/量价/扩产/满产/库存下降/供需），近90天事件计数 → 主题标签：**量价齐升**
    （持续涨价+主题指数20日>0，绿）/持续涨价（青）/价格见顶（90天≥3但30天无新，
    红）/启动（琥珀）。融合进门户产业景气度卡状态列（与财报标签、有色见顶回落
    并列）。**关键坑（2026-08-14 踩过）**：东财搜索 sort=time 返回时间倒序泛
    新闻（前50条仅 2-8 条含关键词，英文词 DRAM/HBM 几乎 0 命中），**必须用
    sort=default 相关度排序**（DRAM 命中率 1/150→22/150）；意图词必须含"上涨/
    飙升/暴涨"（DDR4/DDR5 新闻标题全用这些词，无"涨价"二字）。当前
    （2026-08-14）：存储 62 条/AI硬件 107/半导体 116 全量价齐升、有色 65 条
    量价齐升但期货⚠见顶回落（涨价新闻滞后，双标签并列为真实状态）——固定 T3
    三主题全部'财报高景气+涨价持续'，产业面强支撑。**服务器回退源（2026-08-14）**：
    东财对机房 IP 风控搜索接口（返回 200 但 result 只有 passportWeb 无
    cmsArticleWebOld；本机正常）——搜狗也被反爬（antispider 验证码页），
    **改用腾讯新闻 i.news.qq.com/gw/pc_search/result**（结构化 JSON，
    secList[].newsList[] 含 title/time/url，服务器实测可用、返回精准涨价新闻）。
    **限流应对（用户指示）**：腾讯高频请求触发 ret=400 需验证码 → **每 30 秒
    搜 1 次**（_search 回退分支固定 time.sleep(30)），每产品 1 页 10 条够标签
    判定，全量 49 产品约 25 分钟。**门户涨价追踪卡（2026-08-14）**：bigbull
    产业景气度卡之后新增独立"产业链涨价追踪"卡（读 price_events.json：主题/
    90天/30天/标签/产品事件明细"名=条数"），与本机东财数据上传服务器验证。
    测试：tests/test_price_events.py 6 项（产品词覆盖/标题过滤/标签规则/
    JSON 结构兼容/腾讯回退与解析）。
   - **大周期追溯研究（2026-08-14）**：同一套规则放到历史大周期——新能源池
     （18 只，同花顺后复权日K 2016-08 起）：主升段（2019-2021）模型 +345% vs
     等权 +574%（能赚但只吃六成，MA20 退出在强趋势过早止盈）；回落段
     （2022-2026）模型 +12% vs 等权 -38%（退出保护显著）；全期 +452% vs 等权
     +408% 打平。**白酒池不适用**：全期 -6%（63 笔、胜率 35%、PF 1.17）vs
     等权 +147%——白酒低波动慢牛，T0 信号捕捉脉冲非趋势。**结论：模型适合
     高波动成长板块、不适合低波动慢牛**。数据坑：同花顺**前复权对大额现金
     分红票有 bug**（历史价变负/收益虚高），必须用**后复权**（backward）。
     命令 `mainrise cycle-check`（--fetch-only/--refresh，约1分钟）。
- **仓位**：单票 **1/3 仓**，最多 **3 只**并行（同日多候选按评分/90日计数降序）
- **退出**：**收盘跌破 MA20 卖出**；**不手动买回**——等规则再次触发再入场
  （研究结论：企稳买回为负优化）
- **弱市降档**：大盘等权 20 日涨幅 **≤-5%（杀跌区）不新开仓**；已有持仓按 MA20 退出
- **费用**：0.2%/笔（双边）；T0 信号 = 均线多头 + 创20日新高 +（大阳线涨幅≥5% 且量比≥1.5）
  或（涨停且量比≥1.0）
- 输出：`大牛模型回测_*.md` / **`大牛模型交割单_*.csv`** / `大牛模型净值_*.csv` /
  **门户 `output/web/index.html`（只含模型有效信息）** / 候选 JSON
  `output/state/bigbull_cands.json`（盯盘页实时读取）
- **推送**：每交易日 **14:50 Server酱（方糖）微信推送** 今日买入（盘中 T0 近似：
  涨幅≥5% 且量比≥1.5 或涨停、站上 MA20）/ 卖出（**大牛模型持仓盘中跌破 MA20
  → 收盘确认**，口径与买入一致，框架纸面持仓不再进推送）/ 市场状态；
  **17:30 收盘确认推送**（`mainrise push --close`，读 bigbull 交割单：今日确认
  买入/卖出/当前持仓/大盘状态，收盘口径以交割单为准）；`--dry-run` 只打印不发送
  （不耗配额）；命令 `mainrise push`（key 存 `.serverchan_key`，600，不进 git）

### 框架（用户维护中，2026-08-14 起）

- **第一级 B3（打底仓）**：均线粘合≤3%（MA5/10/20 最大偏离）+ 阳线 + 量比≥2 +
  涨幅≥1% + 收盘站上三均线 + 距60日低点<30% → **明日开盘打底仓（计划仓位 2/3）**
- **第二级 二波（最优买点加仓）**：B3 后 3~30 日内 深回调 2~12% + 均线再次粘合≤2%
  + 缩量 → 放量阳线再启动（触发日收盘判定）→ **明日开盘加仓（1/3，总仓≤1/3）**
- **止损**：买入价 -4%（盘中低点触发；2026-08-13 从 -5% 收紧，用户确认）
- **止盈**：**盘中最高点到收盘价回落 8%**（用收盘价判断，不是盘中低点——曾误用低点导致误平仓）
- **时间止损**：持仓 10-20 个交易日（让赢家跑，B3/二波 回测用 10/20 日对比）
- **防追高**：10 个交易日涨幅 ≥150% 不进买点提示（2026-08 研究：30-80% 强势股胜率更高，由 80% 放宽）
- 仓位：单票 ≤1/3（B3 底仓 2/3 + 二波加仓 1/3），最多 3 只并行，优先综合分 Top10
- **已删除**：原 T0/T1/T2 交易框架（买点1/买点2 回踩企稳）已移除（2026-08-14）；
  T0 信号计算保留（大牛模型依赖）

## 数据与路径

- 数据目录：`$MAINRISE_HOME` 优先；项目根运行用项目内 `data/` + `output/`；独立模式 `~/.mainrise/`
- 本机软链：`~/.mainrise` → `/Users/user/Desktop/chaoduan`（数据复用，不占额外空间）
- 行情：zzshare 免费全市场日线（`data/zzshare_daily/`，2021 起）；同花顺 API 用于财务评估/实时行情（`MAINRISE_API_KEY`）
- 实时行情路由：51/56/58 开头沪市 ETF、15 开头深市基金 → `/api/fund/market/snapshot`；股票 → `/api/a-share/prices/snapshot`
- API Key 保存在 `~/.mainrise/settings.json`（权限 600），GUI 输入一次后自动记忆

## 常用命令

```bash
python3 -m mainrise.cli init       # 首次：交易日历+股票名册+行情（有内置包则只增量）
python3 -m mainrise.cli update     # 增量更新行情
python3 -m mainrise.cli backtest   # 回测生成信号明细（--grid 精细扫描约200组/30-40分钟）
python3 -m mainrise.cli evaluate   # 财务评估（需 MAINRISE_API_KEY）
python3 -m mainrise.cli report     # 综合评分 + 观察池
python3 -m mainrise.cli track      # 每日跟踪报告（--no-scan 跳过全市场扫描）
python3 -m mainrise.cli dashboard  # 以 QuantDark 模板更新 KPI 仪表盘（track 后自动执行）
python3 -m mainrise.cli web        # 生成网页版仪表盘 output/web/index.html（track 后自动执行）
python3 -m mainrise.cli bigbull    # 大牛模型固化回测 + 交割单 + 门户 + 候选JSON（约15秒）
python3 -m mainrise.cli push       # 微信推送（14:50 尾盘决策；--close 17:30 收盘确认；--wecom 企业微信渠道；--dry-run 不发送）
python3 -m mainrise.cli alert "标题" "正文"   # 微信告警（企业微信优先，Server酱兜底；--dry-run）
python3 -m mainrise.cli weekly     # 每周绩效小结推送（交割/净值/信号；--dry-run）
python3 -m mainrise.cli themeindex # 动态热主题（同花顺全A行业指数；--rank-top 10 --lookback 60；--hot 只查当日）
python3 -m mainrise.cli cycle-check  # 大周期追溯研究：新能源/白酒 × 大牛模型规则（约1分钟）
python3 -m mainrise.cli cycle-state  # 市场周期状态卡：当前主线/阶段/模型档位建议（秒级，描述不预测）
python3 -m mainrise.cli entry-study  # 买点提前与胜率研究（约30秒）
python3 -m mainrise.cli bigtrend   # 趋势大牛研究：观察点/买点确认/卖点信号（约30秒）
python3 -m mainrise.cli wave       # 波段高抛低吸研究（约30秒）
python3 -m mainrise.cli bullcnt    # 连续买入信号计数 → 大牛属性判定（约30秒）
python3 -m mainrise.cli candidate-bt  # 大牛候选规则回测（约15秒）
python3 -m mainrise.cli portfolio-bt  # 大牛候选组合级回测：1/3仓+3只上限（约15秒）
python3 -m unittest discover -s tests   # 103 项单元测试
```

**完整流程顺序**（新信号入池必须按序）：更新行情 → 回测 → 财务评估 → 综合评分 → 跟踪报告。
新信号在信号日次日确认后才进信号明细，当天不会入池（设计如此）。

## 关键机制

- 观察池 `output/state/mainrise_watchlist.csv`：由综合评分生成（36 只）；观察池非空时，全市场新信号只列在报告第四节，不占用买点提示区；观察池为空时新信号才兜底进入买点区
- 纸面持仓 `output/state/mainrise_positions.csv`：自动建仓/平仓，可手工编辑（改真实成交价后继续自动管理）；日期列已统一 object 类型
- 报告输出：Markdown + CSV + Excel（6 个 sheet，涨红跌绿），Excel 含持仓盈亏
- **KPI 仪表盘**：`output/reports/主升浪跟踪仪表盘.xlsx` 为最终输出，由
  `mainrise/dashboard.py` 自动更新——模板 `output/reports/主升浪跟踪仪表盘_QuantDark.xlsx`
  （打包软件用内置 `mainrise/resources/dashboard_template.xlsx`），读取全部
  `主升浪跟踪_*.csv` 与持仓 CSV 重建 Data/Watch/Summary/Dashboard 与图表引用；
  `mainrise track` 完成后自动同步，失败不阻塞报告；也可单独 `mainrise dashboard`
  （GUI 有“更新仪表盘”按钮）
- **门户（大牛模型版，2026-08-14 重做）**：`output/web/index.html` 由
  `mainrise bigbull` 生成，**只含大牛模型有效信息**（市场状态/**买卖点提醒卡**
  （今日买点=当日硬规则信号、今日卖出=收盘破MA20、当前持仓含守/破MA20 状态）/
  近期候选信号/净值曲线/最近交割记录/模型规则卡）；候选表**名称+代码合并
  （上名称下代码，代码直达 `/stock/<code>` 看盘页）**且**按代码去重**（取最近
  一次信号）；交割记录**倒序**（最近在前）；**收益曲线与交割为 2026-08-01
  起重置口径**（净值 1.0 起，聚焦 8 月后收益跟踪，KPI 同步窗口口径，完整历史
  回测保留在页脚与 大牛模型回测_*.md，起点常量 `bigbull.PORTAL_REBASE`）；Quant
  Dark、手机自适应；Nginx 直接托管 `output/web/`。旧 dashboard/live/reports 等
  页面文件仍留在磁盘（monitor 服务使用），但门户入口不再链接它们。
- **市场周期状态卡（2026-08-14）**：`mainrise cycle-state`（秒级，每日流水线
  自动更新）——读同花顺全A行业指数缓存（themeindex）+ 上证基准（cycle_ths），
  输出当前**主线**（120/60日相对超额>0 且 60日超额前3，要求持续）、每主题
  **阶段**（启动/主升/高潮/退潮：120日方向优先 + 60日涨幅历史分位）、**模型
  档位建议**（主线∩固定热主题吻合度 → L0 满仓/L1 评分≥3/L2 半仓/L3 停开，
  杀跌区直接 L3）。**只描述当前状态，不预测切换**（前序五项研究：风格检测7
  指标、热主题动态化4方案、机构调研4指标、大周期追溯、研报资源包检索——
  全部证明方向无可靠领先信号，故周期卡不产生买卖信号，仅实时可视化"主线在哪、
  走到哪了"）。输出 cycle_state.json + web/cycle.html + 盯盘页卡（monitor 读
  JSON 注入"大盘状态"卡下方）。实测 2026-08-14：主线为空（8主题60日全负超额）、
  L2 收缩（与模型空仓现状一致）。**机构调研（东财 RPT_ORG_SURVEYNEW）仅保留
  2025-09 起 11 个月**（2024 及更早返回 9201）；**无真实历史研报免费打包**
  （FinRpt 是 LLM 生成的研报；公众号无公开抓取渠道）——研报/调研信号验证均
  无预测力，未接入模型。
  **周期状态回测（2026-08-14，`cycle-state --backtest` 逐日回放 2022-06 起）**：
  周期卡**能识别暴跌但滞后约 2 周**——2026-07 卡点池回撤 -22.5%（6-8月最大
  -24.6%@7-30）期间：7-02~15 误判 L0（大盘反弹+主题60日超额仍正），7-20 才
  L3、7-28 起 L2 持续（躲过 7 月末-8 月下半段）；最早预警信号=**退潮主题数
  ≥5**（7-15 起触发，比 L2 早约 1 周，但用户决定暂不实施——快速指标噪音大
  需重新回测）；6-24 的 L3 是假警报（7 月初反弹证伪）。全期 L3 时段与历史
  下跌吻合（2022-09/2024-01~02/2024-06~07/2025-01/2025-04）。**模型 7 月
  空仓的主保护是模型自身机制**（柯力 07-07 止损后 T0 门槛不入场），非周期卡。
  **上线修复（2026-08-14）**：周期卡注入**门户 index.html**（bigbull 读
  cycle_state.json 渲染"市场周期状态"卡，daily_update 顺序改为 cycle-state →
  bigbull）；**monitor 启动时强制渲染一次 live.html**（不依赖交易时段，重启
  即生效）——手机端 index.html / live.html / cycle.html 三入口均可见周期卡。
- **盯盘页（live.html）**：monitor 每 3 秒轮询，含 **大牛模型候选卡**（实时
  现价/涨跌/量比/距MA20/评分/信号日/状态，**含当前持仓**：状态=持仓/⚠持仓破MA20）；
  候选与持仓数据来自 `bigbull_cands.json`（bigbull 每日落盘，monitor **只读文件
  60s 缓存**——禁止在 monitor 内做全量行情加载）；盘中 T0 近似（涨幅≥5% 且
  量比≥1.5 或涨停、站上 MA20）→ 提醒 + 卡片琥珀高亮"今日或成T0 · 14:50尾盘确认"；
  持仓盘中跌破 MA20 → 提醒"📉 大牛模型持仓 …收盘确认卖出"。
- **微信推送（Server酱）**：`mainrise push`，每交易日 14:50 cron 推送今日买入
  （盘中 T0 近似）/ 卖出（**大牛模型持仓盘中跌破 MA20**，口径与买入一致，框架
  纸面持仓不再进推送）/ 市场状态；**17:30 收盘确认**（`push --close`，随
  daily_update.sh 在 bigbull 后执行，读交割单 CSV + 候选 JSON，只做文件读取）；
  `--dry-run` 只打印不发送（不耗配额）、`--test` 测试；
  key 存 `.serverchan_key`（600，已加入 .gitignore）。
- **企业微信推送（免费不限量）**：`push --wecom` 用企业微信群机器人发 14:50 消息
  （绕开 Server酱 5 次/天）；`alert "标题" "正文"` 通用告警（**企业微信优先、
  Server酱兜底**，`--dry-run` 防误发）；webhook 配置：环境变量 WECOM_WEBHOOK →
  settings.json wecom_webhook → `~/.wecom_webhook`（600，已加入 .gitignore）。
- **运维自动化（2026-08-14）**：`deploy/daily_update.sh` 失败 → 尾日志微信告警
  （EXIT trap）；`deploy/health_check.sh` 工作日 09:05 巡检 monitor 服务/数据新鲜度
  （昨日流水线是否产出，按 trade_dates 判交易日）/磁盘≥85%/内存<300MB → 异常告警；
  `mainrise weekly` 周五 17:35 推送本周买入/卖出/持仓/信号/净值小结；`deploy/
  night_research.sh` 周日 22:30 在服务器跑 8 项研究（entry-study/bigtrend/wave/
  bullcnt/candidate-bt/reversal/launch/strategy）→ output/reports + 完成告警。
- 产业信息在 `mainrise/resources/industry_info.csv`（新增标的信息改 CSV，不用改代码）
- 打包：`build_gui_macos.sh`（内置数据，SKIP_BUNDLE=1 轻量版）、`build_gui_windows.bat`；`PYTHON` 环境变量指定解释器

## 已知决策与坑

- macOS 系统 Tk 渲染空白 → 全部用 PyQt5
- GitHub Actions 打包 Windows：依赖用 `pip install -r requirements.txt pyinstaller`（**必须含 PyQt5**，曾漏装导致 exe 缺界面库）；.bat 用 cmd shell 执行
- PyInstaller 单实例锁 60 秒过期（异常退出不阻塞下次启动）
- 全市场扫描 ~4700 只/组参数，回测默认 18 组约 3-4 分钟
- **monitor 禁止全量行情加载（2026-08-14 OOM 事故）**：曾在 monitor 每 3 秒轮询
  里触发 `load_all_panels()`（700 万行）刷新大牛候选 → 服务器 2C4G 内存耗尽
  OOM 死循环（CPU/内存 90%+、SSH 饿死、需重启）；修复=候选改为 bigbull 每日
  落盘 `bigbull_cands.json`，monitor 只读文件。**教训：盯盘服务内任何全量计算
  都必须移到日频流水线，盘中只做文件读取与快照轮询。**
- 免责：所有输出仅用于研究，不构成投资建议（UI 底部已标注）

## 已安装 Skills（本机）

- `officecli` + `officecli-xlsx` + `officecli-data-dashboard`（OfficeCLI v1.0.143，`~/.local/bin/officecli`）：Excel 仪表盘/文档生成
- `hithink-finance`：同花顺金融数据（REST/MCP/CLI，统一 API Key）

## Excel 仪表盘设计规范（Quant Dark Theme）

输出文件：`output/reports/主升浪跟踪仪表盘_QuantDark.xlsx`（4 sheets：Dashboard / Summary / Watch / Data）

### 配色

| 层级 | 色值 | 用途 |
|------|------|------|
| `#0D1117` | 根背景 | 全工作表底色、空白区域 |
| `#161B22` | 表面 | 数据行交替色、KPI 卡片 |
| `#0F1826` | 标题栏 | 表头行 |
| `#39D2C0` | 量化青 | 页面标题、分区标题 |
| `#58A6FF` | 量化蓝 | KPI 数值、表头底线 |
| `#3FB950` | 绿 | 上涨/买入信号 |
| `#F85149` | 红 | 下跌/卖出信号、折线图 |
| `#D29922` | 琥珀 | 买点提示>0 高亮、多头持有 |
| `#BC8CFF` | 紫 | 柱状图辅助色 |
| `#E6EDF3` | 主文字 | 数据行 |
| `#8B949E` | 次级文字 | 标签、列标题 |
| `#6E7681` | 脚注 | 免责声明 |

### 字体

**Consolas** 等宽字体，全表统一。层级：KPI 数值 28pt bold 纯白 `#FFFFFF`，标题 13pt bold 量化青，表头 10pt bold 次级色，数据 11pt 主文字色，脚注 9pt 脚注色。

### KPI 卡片

- 6 个 KPI 位于 Dashboard Row 2（标签）+ Row 3（数值），Column **B 起始**（A 列留白做左侧负空间）
- 标签与数值共享 `#161B22` 卡片底色，仅 hair 边框 + 底部 medium 语义强调色
- 6 个强调色：蓝(计数)、青(池)、琥珀(买点)、蓝(综合分)、绿(涨跌幅)、绿(上涨家数)

### 表格

- **无竖线** + hair 横分隔线 + 交替行色（`#0D1117`/`#161B22`）
- 表头仅底部 thin 强调线（`#58A6FF`），无其他边框
- 分区标题左对齐，底部 thin 分隔线

### 图表

- 背景 `#0D1117`（非透明），网格线 `#21262D` 极淡，坐标轴 `#30363D`
- 标题 `#E6EDF3`，标签/图例 `#8B949E`
- **折线图用红色 `#FF0000`**（国内红涨惯例，用户偏好），线宽 2.25pt
- 柱状图用量化青 `#39D2C0` + 量化紫 `#BC8CFF`
- 图表 XML 修改方法：保存为 .xlsx（ZIP）后直接替换 `xl/charts/chartN.xml`

### Summary 状态分布

透视表格式：日期为列头（`=$A$2`/`=$A$3`/`=$A$4`），状态为行头（空头/破位/多头持有/回踩低吸/多头回踩/T1确认买点/T0新信号），COUNTIFS 公式填充。宽表格式比纵向列表信息密度更高。

### 已知待改进

- Data 和 Watch 的条件格式存在新旧两套规则叠加（原始 `...G1000` + 新增 `...G2000`），需去重
- Summary 部分脚注/辅助文字颜色尚未统一到暗色主题色板（`#1F4E79`/`#666666`/`#A6A6A6` 残留）
- Watch 涨跌幅数值的小数位格式（`0.00`）未全覆盖

## 会话记忆（2026-08-07）

本轮完成：12 条代码改进（清理 src/config/Tk 重复代码、API 单例线程安全、产业信息 CSV 化、回测 --grid、19 项单元测试、打包脚本 PYTHON 支持）；API Key 记忆保存；持仓账本 FutureWarning 修复；平仓规则改为收盘回落 8%；GitHub 推送与 .app 重新打包；安装 officecli skill 链；**Excel 仪表盘量化暗色主题全量重设计**（4 sheets 配色/字体/卡片/表格/图表暗化，含图表 XML ZIP 级修改和用户手动精调）。

本轮新增：**仪表盘自动更新功能**（`mainrise/dashboard.py`，模板为
`主升浪跟踪仪表盘_QuantDark.xlsx`/内置 `mainrise/resources/dashboard_template.xlsx`，
输出 `output/reports/主升浪跟踪仪表盘.xlsx`）：`mainrise track` 完成后自动同步
（`--no-dashboard` 可跳过，失败不阻塞），独立命令 `mainrise dashboard`，GUI 报告组
新增“更新仪表盘”按钮；更新器重建 Data/Watch/Summary/Dashboard 并 ZIP 级替换图表
XML（只改引用、保留暗色样式），Summary 各区块随天数自动下移；4 个构建脚本均已
打包模板资源；新增 tests/test_dashboard.py 2 项测试（合计 21 项）。

本轮收尾：观察池数量与模板行数改为动态推导（不再写死 36/192，新增
`test_dynamic_watchlist_size`，合计 22 项测试）；`sys._MEIPASS` 兜底模板解析；
macOS `dist/主升浪跟踪.app` 已重新打包（含内置行情 259MB + 仪表盘模板，
`Contents/Frameworks/mainrise → Resources/mainrise` 符号链接可达）。Windows exe
需在 GitHub Actions / Windows 上跑 `build_gui_windows.bat` 或推送触发。

## 会话记忆（2026-08-08，UI 轮）

本轮完成：**Apple 风格 UI 重设计 + 启动画面 + 应用图标**（规范参考
`/Users/user/Desktop/PY/MC_LogAnalysis/PROJECT_MEMORY.md` 界面章节）。

- GUI（`mainrise/gui_pyqt.py`）改为 Apple 卡片风：背景 `#F5F5F7`、白卡片圆角 8px、
  主色 `#007AFF`、间距常量 `GAP_SECTION/ROW/INNER/CARD_PAD`；窗口按屏幕可用区域
  **80%** 动态计算（`window_target_size`，最小 900×620）；`AA_EnableHighDpiScaling`
  与 `AA_UseHighDpiPixmaps` 在 QApplication 创建前启用（Windows 高分屏清晰）。
- 启动画面：`mainrise/resources/splash.png`（1280×800 浅色 Apple 风，logo + 标题 +
  红色点缀），与主窗口同尺寸 cover 裁切填满，`SPLASH_MIN_MS=1000` +
  `FADE_MS=300` 淡出衔接主窗口；无数据引导弹窗延后到过渡结束后（避免压住淡出）。
- 图标：由根目录 `zhushenglang.png` 派生 `mainrise/resources/app_icon.png`（1024）、
  `app.ico`（Windows）、`app.icns`（macOS，iconutil 生成）；两个打包脚本已加
  `--icon` 与 add-data（icon/splash）。
- 「使用说明」右上角 link 按钮；状态栏右下角版权 `© 2026 主升浪信号跟踪`；帮助菜单
  新增「关于」；主按钮：更新行情 / 生成跟踪报告 / 查询行情。
- macOS `dist/主升浪跟踪.app` 已重新打包（465MB，含图标 + 启动画面 + 内置行情 +
  仪表盘模板，离屏启动验证 UI ready 0.24s）。Windows exe 仍需在
  GitHub Actions / Windows 跑 `build_gui_windows.bat`。
- 22 项单元测试全绿（`tests/test_settings.py` 导入不受影响）。

## 修复（2026-08-08，按钮重叠）

- 现象：窗口偏矮时左侧功能面板被压扁，按钮缩成 1-15px 细条堆叠，视觉上像重叠。
- 根因：5 个 QGroupBox 纵向按钮列内容 ~600px，而常见屏幕 80% 窗口下左面板仅
  ~460px 高，布局把按钮压缩到低于最小高度。
- 修复：左侧改为**单张功能卡片 + 两列按钮网格**（10 个操作按钮 5 行 + 实时行情
  输入/查询行），内容高度降至 ~345px；按钮 QSS 加 `min-height: 30px`（任何尺寸
  不被压缩）；外层保留 QScrollArea 兜底（正常尺寸不出现滚动条）；左面板最小宽度
  290 保证两列按钮文字不裁切。按钮文案精简为：初始化全量数据/更新行情/回测/
  财务评估/综合评分/生成跟踪报告/更新仪表盘/报告目录/最新跟踪报告/Excel 报告。
- 验证：900×620 ~ 1512×982 五档尺寸按钮高度均 42-44px、零重叠、零裁切、
  无滚动条；22 项测试全绿；`dist/主升浪跟踪.app` 已重新打包（UI ready 0.25s）。

## 修复（2026-08-10，按钮排列均匀化）

- 现象：功能卡片撑满面板后，多余高度被布局塞进标题项（标题 item 高 149px、
  文字仅 21px），导致标题下大空白、按钮全部挤在底部。
- 修复：卡片内改为**逐行 QHBoxLayout + 行间 `addStretch(1)` 均分**：标题固定在
  顶部、6 行按钮（每行两列，按钮固定 42px；后新增"网页仪表盘/打开网页"一行）
  随窗口高度等距散布、实时行情
  输入/查询行固定在底部；不再依赖 QGridLayout 行拉伸（Qt 分配行为不稳定）。
- 效果：900×620 档行距约 8-9px，1512×982 档约 71-72px，五档尺寸行距等比
  均匀、无顶部空档、无滚动条；22 项测试全绿；`dist/主升浪跟踪.app` 已重新
  打包（UI ready 0.38s）。

## 会话记忆（2026-08-10，网页仪表盘 + 腾讯云部署）

本轮完成：**网页版仪表盘 + 腾讯云每日自动更新部署方案**。

- 新增 `mainrise/web_dashboard.py`：读 `output/reports/主升浪跟踪_*.csv` +
  `output/state/`（持仓/观察池），渲染单文件自包含 HTML（Quant Dark 配色、
  mobile-responsive、无外部依赖），输出 `output/web/index.html`。区块：8 个 KPI
  卡片（天数/池/买点/平均分/平均涨跌/上涨家数/活跃持仓/新信号）、今日买点、
  持仓管理（活跃/待买/已平仓+盈亏）、观察池、全市场新信号、每日概况、状态分布、
  免责脚注；国内红涨绿跌。
- `paths.py` 新增 `web_dir()`（项目模式 `output/web/`，独立模式 `~/web`）；
  `cli.py` 新增 `web` 子命令，`track` 后自动同步（与 dashboard 同模式，
  `--no-dashboard` 一并跳过）；GUI 功能卡新增“网页仪表盘 / 打开网页”按钮。
- 部署物料：`requirements-server.txt`（去掉 PyQt5/akshare/matplotlib 等未用包）、
  `deploy/daily_update.sh`（update→backtest→evaluate→report→track→web，cron
  样例、API Key 读取、按日日志）、`deploy/nginx_web.conf`（静态托管 + Basic Auth
  注释）、`deploy/README.md`（Ubuntu 步骤：venv/init/cron/Nginx/HTTPS/FAQ）。
- **全量数据打包上传**：新增 `deploy/package_data.sh`（本机把 `data/zzshare_daily`
  1356 天 + stock_list/trade_dates + output/state 打成 `build/server_data.tar.gz`，
  实测 656MB→256MB），scp 上传后服务器 `tar -xzf` 解压即用，`init/update` 只做
  增量补齐，不下载全量；服务器带宽 1Mbps 也够用。
- 选型结论：BF1 2核4G 包年包月 3 年（约 35 元/月），不用竞价实例（每日任务不可
  中断）；`deploy/README.md` 含腾讯云购买配置清单（Ubuntu 22.04、50GB 盘、
  1Mbps 固定带宽、安全组 22/80/443）与服务器初始化命令（含 4G swap）。

## 部署实录（2026-08-10，腾讯云上线）

- 服务器：腾讯云 CVM 蜂驰型 BF1 2核4G，Ubuntu 22.04，公网 129.28.95.59，
  SSH 用控制台密钥 `~/Downloads/Zhushenglang.pem`（**需 chmod 600**），
  登录用户 `ubuntu`（root 密钥登录被禁），sudo 免密可用。
- 全量数据走打包上传：本机 `deploy/package_data.sh` 生成 256MB tar.gz →
  scp → `tar -xzf`，服务器不下载；数据 1356 天 + 持仓/观察池全部带过去。
- 一键初始化 `deploy/setup_server.sh`：装依赖 + 4G swap + venv（清华 pip 镜像）
  + 完整流水线 + cron + Nginx。
- **踩坑 3 个（已修）**：
  1. `get_api_key()` 只读环境变量，setup 脚本未导出 `.api_key` → evaluate 全部
     0 分、重点线索为空、report 崩（KeyError: 'code'）。修复：setup_server.sh
     在跑流水线前 `export MAINRISE_API_KEY`；report.py 重点线索为空时回退观察
     线索、仍为空则给出明确报错。
  2. 第一次流水线以 root 跑，`output/` 归 root；再以 ubuntu 重跑 PermissionError。
     统一以 root 跑（cron 也是 root），或先 chown。
  3. Nginx：默认站点 default_server 抢占 80 → 删 `sites-enabled/default`；
     `/home/ubuntu` 权限 750 → `chmod o+x /home/ubuntu`，否则 www-data
     stat() 403/404。
- 现状：http://129.28.95.59/ 已上线（Basic Auth：zhushang/随机口令），数据
  截止 2026-08-10，cron `30 17 * * 1-5` 跑 daily_update.sh（root），日志在
  `output/logs/daily_*.log`。尚未配 HTTPS 与失败告警（可后加）。
- 网页登录用户已改为 `zhusheng`（口令由用户自定义；改密命令：
  `sudo htpasswd -b /etc/nginx/.mainrise_htpasswd zhusheng 新密码`；明文密码
  不写入记忆/仓库，防止泄露）。

## 功能（2026-08-10，盘中实时盯盘网页版）

- 新增 `mainrise/monitor.py` + CLI `mainrise monitor`：交易日盘中（北京时间
  9:30-11:30 / 13:00-15:00，用固定 UTC+8，不受服务器时区影响）每 30s 轮询
  同花顺实时行情，监控 持仓+观察池（读 state CSVs，名单自动跟随每日流水线）。
- 提醒规则：持仓跌破止损-4%（低点触发）/ 自峰值回落8% / 观察池涨跌幅±5% /
  现价回踩 MA10/MA20（最新日线值±1%）；每只票同类 10 分钟限频；接口失败退避。
- 输出 `output/web/live.html`（meta 30s 自动刷新，Quant Dark 风格，手机可用，
  沿用 Nginx 口令）+ `live.json`；仪表盘 index.html 导航新增"实时盯盘"入口。
- 测试：`tests/test_monitor.py` 3 项（交易时段/提醒规则+限频/状态构建+渲染），
  合计 27 项全绿。
- 服务器部署：systemd 服务 `deploy/mainrise-monitor.service`（User=ubuntu，
  EnvironmentFile=`.monitor_env` 读 API Key，Restart=always，开机自启），
  `output/` 已 chown 给 ubuntu；`deploy/README.md` 第九节含安装步骤。
- 上线验证：41 只监控、3 条真实盘中提醒（宏和科技-5.5%、东鹏饮料+6.6%、
  桂冠电力回踩MA10），live.html 公网 200 可访问。推送渠道未接（用户暂选网页版）。

## 功能（2026-08-10，门户分页入口）

- `output/web/index.html` 改为**门户首页**：登录后 4 个大按钮分别进 KPI 仪表盘
  （dashboard.html）/ 实时盯盘（live.html）/ 每日报告（reports.html）/ 使用说明
  （about.html），Quant Dark 风格，手机自适应。
- `web_dashboard.py` 扩展：`_md_to_html` 极简 md→HTML（标题/表格/引用/列表，
  先转义防注入），每份 `主升浪跟踪_*`、`主升浪信号综合评估_*`、`信号评估_*` md
  渲染为 `reports_view/<stem>.html` 静态页，`reports.html` 按三类分组列出；
  `update_web_dashboard` 一次生成 index/dashboard/reports/about + reports_view。
- 各页导航互通：dashboard/live 均有"首页"链接，live 另有"KPI仪表盘"入口。
- 注意：zzshare 当日日线收盘后才发布，盘前/盘中跑 track 会用上一交易日（如
  8/10 上午报告仍是 8/07）；17:30 定时任务才是当日正式报告。实时盯盘不受影响。
- 测试：`test_web_dashboard.py` 更新为校验门户/仪表盘/报告页，27 项全绿；
  服务器已同步并重启盯盘服务，公网验证 portal/dashboard/reports/报告详情页
  全部 200。

## 修复（2026-08-10，股票名称"待补"）

- 现象：观察池/网页里部分股票中文名显示"待补"（41 只中 6 只）。
- 根因：`report.py` 只给产业信息表内的代码填名字，其余留在 NaN →
  `fillna("待补")`；`tracker.py` 读取观察池时 `r["name"] or names.get(...)`
  把"待补"当有效值，不再回退股票名册。
- 修复：`report.py` 名字先回退 `signals.load_names()`（stock_list.csv）再兜底；
  `tracker.py` 三个分支统一 `names.get(code)` 优先、观察池名字仅在非"待补"
  时兜底；`monitor.py` 同样回退名册，避免盯盘页出现"待补"。
- 验证：服务器重跑 report+track 后观察池 41 只待补=0（如 002721→金一文化），
  dashboard/live/reports 公网页面"待补"全部为 0；27 项测试全绿。

## 功能（2026-08-10，当日复盘）

- 新增 `mainrise/review.py` + CLI `mainrise review`：输出 `output/web/review.html`
  + review.json，门户首页新增"当日复盘"按钮；`track` 后自动同步，`daily_update.sh`
  兜底执行。
- 数据源（同花顺 REST，已在服务器实测）：涨停池 limit-up-pool（含
  continue_day_cnt 连板数 / limit_up_reason 题材 / limit_up_time / seal_money）、
  连板天梯 limit-up-ladder（30日矩阵，未使用——实测其末日期停留在 2026-06-30，
  疑似上游滞后）、龙虎榜 dragon-tiger-list（org_net_value 机构净额 /
  hot_money_net_value 游资净额 / concept_list 概念）。
- 内容：①连板梯队按连板数分组（首板~最高板，含涨停时间/封单/题材）；
  ②按题材分组 Top12（涨停原因关键词分词：+、，/；等）；③短线情绪周期
  （涨停家数/最高板/连板家数/首板晋级率/10日均值 → 冰点/启动/发酵/高潮/退潮/
  震荡启发式判定）；④12 日情绪趋势 SVG 双线折线图（涨停家数+最高板）；
  ⑤龙虎榜资金（机构/游资净额，口径=当日上榜股）。
- **数据边界（不造假）**：同花顺 REST 33 个端点无"全市场主力/大户/散户资金
  流向"接口，故无该折线图；页面已明确标注，未用近似/模拟数据替代。龙虎榜
  盘中为当日数据（收盘后完整），机构净额盘中可能为 0。
- 服务器实测（2026-08-10 盘中）：涨停 66 家、最高 5 板、情绪"发酵"；
  题材 Top：创新药(9)/中报预增(4)/黄金(3)；龙虎榜 08-10 盘中暂无数据（0）。
- 测试：`tests/test_review.py` 2 项（分组/情绪/渲染 mock API、题材分词），
  合计 29 项全绿。

## 功能（2026-08-10，资金流卡片 · 东方财富）

- 同花顺 REST 无资金流端点，改接**东方财富公开接口**（push2.eastmoney.com，
  实测真实可用，非模拟）：
  - 全市场资金流：`/api/qt/stock/fflow/kline/get`（klt=1 分钟线，secid
    1.000001 上证 + 0.399001 深成，字段 f52主力/f53小单/f54中单/f55大单/
    f56超大单，当日**累计值**，两指数按分钟相加=沪深全市场）；口径校验：
    主力=超大+大单 ✓。
  - 概念板块资金流：`/api/qt/clist/get?fs=m:90+t:3+f:!50&fid=f62`（504 个
    概念板块，按主力净流入排序）；中单 = -(主力+小单) 推导。
- 复盘页新增两张卡：①资金流向（5 线 SVG 累计折线图 + 主力/超大/大/中/小单
  净值 KPI）；②概念板块资金流 Top15（板块/涨跌%/主力/超大/大/小单/占比）。
  实时盯盘页新增"概念板块资金流 Top10"卡（每 60 秒缓存刷新一次，随 30s 轮询
  更新）。口径：超大+大=主力、中≈大户、小≈散户（东财口径，页面已标注）。
- **东财稳定性处理（重要）**：免费公开接口会随机断开连接
  （RemoteDisconnected），已做：双主机回退（push2/1.push2）+ 重试 +
  60s 缓存 + 失败保留上次数据 + 错误写入 live.json/concepts_error.log；
  卡片不会因偶发断连变空。
- 服务器实测（2026-08-10 盘中）：主力净流入 -446.6 亿（超大 -299.3、
  大 -147.4、中 +114.1、小 +332.5），109 个分钟点；概念 Top：高带宽内存
  18.3 亿、味蕾经济、消费风格、中芯概念、创新医疗服务。
- 测试：`tests/test_review.py` 新增东财取数测试（两市场叠加/主力=超大+大/
  中单推导），合计 30 项全绿。

## 功能（2026-08-10，盯盘页排序 + 标的详情分时/K线）

- **盯盘页排序**：`live.html` 表头可点击排序（代码/名称/现价/涨跌%/今高/今低/
  MA10/MA20），外加「按涨跌幅」「按现价」快捷按钮；JS `sortT` 实现，排序状态
  存 URL hash（`#sort=4:a`），30s 自动刷新不丢排序。
- **标的详情页**：盯盘页点代码进入 `/stock/<code>`（Nginx 反代
  `127.0.0.1:8765`，monitor 进程内嵌 stdlib HTTP 服务，同一 Basic Auth）：
  日K（同花顺 `/prices/historical` 前复权近 120 根，SVG 蜡烛图+成交量，稳定）+
  分时（东财 `trends2`，SVG 价格线）；每标的 60s 缓存，用户打开才取数；
  东财失败时该区块降级提示、日K 不受影响。
- **快照批量优化**：`snapshot.fetch_snapshot` 股票改为单请求逗号批量
  （41 次→1 次，实测 0.6s），盯盘轮询提速。
- **午间休市说明**：盯盘服务在 11:30-13:00 不轮询（设计如此，每分钟检查），
  13:00 自动恢复；排查"服务不更新"先看是否午休/收盘。
- **东财限流实录**：频繁请求后 push2/1.push2/push2his 的 API 路径对服务器 IP
  返回空/断连（主机可达、接口 000），>45 分钟未恢复；已通过 60s/300s 缓存+
  后台线程+降级保证页面不闪空。行业板块卡/资金流/分时待东财恢复后自动回填；
  复盘页需恢复后重跑 `mainrise review`（当前保留旧概念板块数据）。
- 测试：新增 `tests/test_stockpage.py` 2 项（日K/分时取数与降级、页面渲染），
  快照批量与排序断言并入 monitor/review 测试，合计 32 项全绿。

## 功能（2026-08-10，全站股票代码可点 → K线/分时）

- `web_dashboard.CODE_LINK_JS` 注入所有页面（`_page` 与 `_render_html` 两个外壳）：
  页面加载后把表格里 6 位数字单元格自动包成 `/stock/<code>` 链接（已含链接的
  单元格跳过），仪表盘/复盘/每日报告/报告详情/门户全部生效，后续新页面自动覆盖。
- 容错修复：`review` 在东财不可用时不再整体失败（`_safe_sector_flow` 返回空，
  行业板块/资金流卡显示"暂不可用"，恢复后重跑 `mainrise review` 自动回填）。
- 验证：6 个页面 `isCode` 脚本均已注入；`/stock/<code>` 详情页 200；下午盘
  13:00:44 盯盘已自动恢复轮询（41 只）。32 项测试全绿。

## 修改（2026-08-10，板块卡改看总资金）

- 行业板块资金流卡改为**按总资金=成交额（f6）排序**展示（原按主力 f62 筛选）：
  `fetch_sector_flow` 改 `fid=f6` + 字段 f6，items 增加 `total`（f6/1e8 亿）；
  复盘页显示 板块/涨跌%/总资金(成交额)/资金净额(主力)/主力净占比，盯盘页显示
  板块/涨跌%/总资金(亿)/资金净额(亿)。实测电子 5641 亿、半导体 2856 亿、
  医药生物 1730 亿。
- 东财数据缓存：`update_review` 把上次成功的 fund_flow/concept_flow 存
  `output/web/.flow_cache.json`，东财取数失败时沿用缓存并在页脚标注
  "缓存数据"（页面不降级为空），恢复后重跑自动更新。
- 东财限流仍在间歇出现（时好时坏），盯盘页每 5 分钟自动重试、复盘页缓存兜底，
  均无需人工干预。32 项测试全绿。
- 板块卡追加**资金净额**列（= 主力净流入净额，东财板块资金流标准净额口径）：
  复盘页显示 板块/涨跌%/总资金(成交额)/资金净额(主力)/主力净占比；盯盘页显示
  板块/涨跌%/总资金(亿)/资金净额(亿)。实测前排：电子 总资金 5683 亿、
  净额 -253 亿。

## 整理（2026-08-10，清理无用文件）

- 流程：盘点 → 移入 `_archive/` → 全量验证（32 测试/网页/仪表盘/CLI/GUI/构建
  脚本依赖均正常）→ 删除 `_archive`（共 1.3GB）。
- 已删除（gitignore 类产物，不影响 git 仓库）：`build/`（bundled_data.zip、
  server_data.tar.gz 等 634MB，可 `./deploy/package_data.sh` / 构建脚本再生）、
  `dist/mainrise`、`dist/主升浪跟踪`（非 .app 的 PyInstaller 目录，仅保留
  `dist/主升浪跟踪.app`）、`mainrise.spec`/`主升浪跟踪.spec`、
  `mainrise_quant.egg-info/`、`.trash_20260805/`（198MB）、`.DS_Store`、
  `output/reports/主升浪跟踪仪表盘_改进版.xlsx`（旧模板，QuantDark 模板保留）。
- 已删除（git 追踪文件，README 同步更新）：`build_macos.sh`/`build_windows.bat`
  （旧 onefile 构建，被 build_gui_* 取代）、`scripts/backtest_mainrise.py`/
  `evaluate_signals.py`/`track_mainrise.py`/`build_full_report.py`（旧命令兼容壳，
  等价 mainrise 子命令）、`docs/pinggu.txt`（旧工作笔记）。
- 保留：`scripts/build_bundle.py`（build_gui_macos.sh 依赖）、
  `output/reports/主升浪跟踪仪表盘_QuantDark.xlsx`（dashboard.py 模板）、
  `zhushenglang.png/.ico`（logo 源文件）、`.env.example`（Key 配置说明）。

## 备份（2026-08-10，GitHub Backup 仓库）

- 备份仓库：`github.com/Longtianhong88888/Backup`（私有，SSH 推送 main 分支，
  提交 dcbb2d7，53 个文件 16MB）。
- 内容：当前目录全量（代码 mainrise 24 项 / deploy 6 / tests 10 / output 报告
  与网页 / README / AGENTS / 构建脚本 / logo 源文件 / .env.example），
  **不含** `data/`（行情数据，可 init/update 重建）与 `dist/`（.app 含 258MB
  内置数据包，超 GitHub 单文件 100MB 上限且可 `build_gui_macos.sh` 重建）。
- 备注：本机 `/tmp/Zhushenglang-backup_2026-08-10.tar.gz`（596MB）为含行情与
  .app 的全量压缩备份（/tmp 重启会清空，需要可移到别处）；GitHub 大文件若需
  备份可走 Release 附件（上限 2GB）。

## 功能（2026-08-10，门户全页面刷新按钮）

- 门户每个页面右下角新增「🔄 刷新」悬浮按钮：点击 POST `/refresh?page=...`
  触发**服务器端重新生成**后自动重载（不只是浏览器重载静态页）。
- 实现：复用 monitor 内嵌本地服务（127.0.0.1:8765），`stockpage.refresh_page`
  分发——`web`=重新生成全部网页（web_dashboard）、`review`=重新生成当日复盘
  （review，约 20s）、`live`=立即跑一轮盯盘（monitor.run_one_cycle）；
  Nginx 新增 `location /refresh` 反代（同一 Basic Auth）；标的详情页刷新按钮
  走 `/stock/<code>/data?force=1` 绕过 60s 缓存强制取数。
- `web_dashboard._refresh_ui(page)` 注入所有页面（`_page` 支持 refresh 参数，
  review 传 `refresh="review"`；`_render_html` 固定 web；live 用 `refresh="live"`）。
- 踩坑：Handler 原本只实现 GET，按钮 POST 导致 501 → 补 `do_POST` 同路由；
  部署 Nginx 配置必须 sed 路径替换（教训已在记忆）。
- 验证：三个端点公网 200（web 0s / live 0.2s / review 22.7s），live.html 与
  各静态页均含 hardRefresh，标的 force 刷新返回新数据；33 项测试全绿。

## 功能（2026-08-10，K线预热 + 盘中每分钟续刷）

- 目标：门户所有代码点开即出 K线（不等缓存），盘中数据每分钟保持新鲜。
- 实现（monitor 新增 `_warm_loop` 预热线程，盘中运行）：
  - **全量 K线**：开盘后首轮 + 每 10 分钟全量预热所有监控代码（同花顺日K，
    约 41 请求/10min，避免触发限流）→ 点开秒出。
  - **热门标的**：最近访问过（30 分钟内、≤15 只）的代码**每分钟**续刷
    K线 + 分时。
  - 详情页本身每 60 秒自动刷新；接口失败静默跳过、缓存保留旧数据。
- 首次点击提速：`fetch_minute_trend` 改走 `_em_fast`（push2his 单主机单次、
  6s 超时快速失败），不再因东财重试阻塞 K线展示（实测首次取数 0.5s、
  预热缓存 0.5s）；`warm_kline/warm_trends` 增量合并进缓存，
  `recent_viewed()` 跟踪热标的。
- 说明：41 只全量每分钟抓取会把同花顺/东财打限流（今日实测 429/断连），
  故采用"全量 10 分钟 + 热门 1 分钟"的折中，效果上你正在看的票每分钟都是新的。
- 测试：`tests/test_stockpage.py` 新增预热合并/热标的测试，合计 34 项全绿。
- **频率调整（用户确认）**：更新频率统一降为 **5 分钟**（没人一直盯着看）——
  `_warm_loop` 改为每 300s 全量刷新所有监控代码日K + 热门标的分时；
  标的详情页自动刷新 60s → 300s（文案同步）。实时盯盘行情仍 30s（提醒机制不动）。
  34 项测试全绿。
- **东财频率再降（用户确认，配合限流）**：东财数据统一**每 10 分钟获取一次**——
  板块资金流缓存 300s→600s、`_warm_loop` 分时续刷改为 10 分钟一轮
  （同花顺日K 仍 5 分钟）、标的详情页数据缓存 CACHE_TTL 60s→600s
  （点开优先用缓存，force 刷新按钮可绕过）；盯盘页板块卡文案改"10分钟刷新"。
  34 项测试全绿。
- **数据源切换（腾讯优先，东财/同花顺兜底）**：腾讯接口实测稳定
  （qt.gtimg.cn 实时 / web.ifzq.gtimg.cn 分时 minute/query、日K fqkline /
  proxy.finance.qq.com 行业板块 getRank，响应 0.2-0.3s，无限流）：
  - 分时：`_tx_minute` 腾讯优先，`_em_fast` 东财兜底
  - 日K：`_tx_kline` 腾讯优先，同花顺 historical 兜底
  - 行业板块资金：`_tx_sector_flow` 腾讯优先（31 个大行业，turnover 成交额+
    zljlr 主力净流入，万→亿），东财 clist 兜底
  - **例外**：全市场资金流 5 线折线图（主力/超大/大/中/小单）腾讯无此粒度数据，
    仍走东财 + 缓存兜底（10 分钟节奏）
  - 实测：K线 121 根 + 分时 267 点全部腾讯源、无错误。36 项测试全绿。
- **看盘软件风格图表（标的详情页）**：重写 `PAGE_JS`——分时：现价线（红涨绿跌）
  + 黄色均价线（累计额/量）+ 昨收灰虚线 + 涨跌面积 + 成交量柱 + 时间网格 +
  右侧价格轴 + 十字光标（时间/价/均价/涨跌%/量）；K线：红绿蜡烛 +
  MA5/10/20/60 均线 + 成交量柱 + 日期轴 + 右侧价格轴 + MA 图例 +
  十字光标 OHLC 提示。腾讯分时数据补 volume/amount 字段（均价线数据源），
  15:30 收盘后快照点过滤（显示到 15:00）。数据源说明更新为
  "K线/分时=腾讯优先，同花顺/东财兜底"。36 项测试全绿。
- **K线信号标注（T0/T1/T2 红色箭头）**：读取回测明细
  `output/reports/mainrise_trades.csv`（S_date=T0、buy_date=T2，T1=信号日
  次日，按 K线窗口日期索引推导），`build_stock_data` 输出 `signals` 数组
  （最多最近 5 组）；K线图每个触发日在 K线顶部画红色箭头 + T0/T1/T2 文字
  标注（SVG marker 箭头，图表顶部留 12% 空白放箭头）。文件按 mtime 缓存
  （`_signals_for`，回测重跑后自动刷新）。实测 601899 标注两组信号。
  37 项测试全绿。
- **K线平仓标注（绿色箭头）**：读取纸面持仓
  `output/state/mainrise_positions.csv`（status=closed 的 close_date+reason，
  `_closes_for` 按 mtime 缓存），K线图在平仓日画绿色箭头 + "平" 标签，
  图表顶部图例显示平仓日期与原因（如"平仓：08-05 高点回落8%"）。
  实测 600489 平仓 08-05 标注正常。37 项测试全绿。
- **热门概念板块卡（腾讯 · 特别关注）**：新增 `_tx_concept_flow`
  （腾讯 gn 概念板块 200+，自动过滤 昨日涨停/新股/ST 等非题材项）+ 特别关注
  主题匹配卡：AI硬件/半导体设备与材料/存储/商业航天/机器人/创新药/食品饮料/
  有色 共 8 个主题，按**关键词优先级**匹配（取首命中关键词中总资金最大者），
  概念板块无匹配时**行业板块兜底**（如创新药→医药生物，标注"行业"）；
  下方附概念板块 Top10（按总资金）。实测匹配：半导体产业 5404亿、
  共封装光模块 4674亿、存储器 3959亿、机器人概念 5869亿、
  有色金属概念 2799亿、医药生物(行业) 2105亿等。39 项测试全绿。
- **修复（部分代码点不进详情页）**：根因是代码链接靠前端 `CODE_LINK_JS`
  运行时转换，部分浏览器/缓存环境下不生效。改为**服务端直接生成链接**
  （`web_dashboard._code_link` 用于今日买点/持仓/观察池/新信号表，
  review 连板/题材/龙虎榜表），不依赖 JS；`CODE_LINK_JS` 保留作报告页
  （reports_view md 表格）兜底。Nginx `location /` 加
  `Cache-Control: no-store` 防浏览器缓存旧版页面。已验证 dashboard 中
  002793/600489、review 各代码均为服务端直链。37 项测试全绿。
- **修复（每日定时任务没跑，2026-08-10 20:0x 排查）**：17:30 cron 执行
  daily_update.sh 报 `/bin/sh: Permission denied`——脚本上传后丢失执行权限
  （644）。已 `chmod +x`（本地+服务器，git 记录执行位）；手动补跑全流水线
  成功：8/10 跟踪报告/仪表盘/网页/复盘全部生成（20:14）。**教训：部署 .sh
  后必须确认 +x，否则 cron 静默失败**。
- **K线蓝色趋势线**：`drawK` 新增线性回归趋势线（20 根实线 + 60 根虚线，
  蓝色 #58A6FF，含 ±1σ 通道）+ 支撑/压力摆动点连线（最近 60 根局部低点/
  高点连线下蓝点标注）；图表说明同步更新。39 项测试全绿。
- **修复（分时图缺失）**：① 腾讯分时成交量单位为"手"，均价线改为
  `ca/(cv*100)`（之前会把均价算成 3517 拉爆比例）；② lo/hi 增加 isFinite
  过滤防 NaN 空白；③ `warm_kline` 新建缓存条目时连同分时一起取，不再产生
  "只有K线缺分时"的残缺缓存；④ `get_stock_data` 命中残缺缓存（有K线无分时）
  时自动完整重建；⑤ 两个 SVG 显式 `height:auto;display:block` 保证移动端
  渲染高度。已验证非 force 请求 kline 121 + trends 242。36 项测试全绿。
- 腾讯云路径：轻量服务器 2C4G → git clone + venv + `pip install -r
  requirements-server.txt` → `mainrise init`（约 650MB）→ cron 工作日 17:30 跑
  `deploy/daily_update.sh` → Nginx 托管 `output/web/` → 手机浏览器/添加到主屏幕。
- 测试：新增 `tests/test_web_dashboard.py` 2 项（生成含关键区块/数据、空数据不
  报错），合计 24 项全绿；本机 `mainrise web` 用真实数据生成 19.5KB HTML 验证
  通过；`dist/主升浪跟踪.app` 已重新打包（UI ready 0.25s）。

## 会话记忆（2026-08-13，数据源全迁公开接口 + 盯盘秒级更新）

### 数据源迁移：腾讯为主 + 东财补缺口，彻底去掉同花顺（无需 API Key）

评估结论：**不能全换腾讯**（腾讯公开面只有行情/排名类；财务/涨停池/龙虎榜/
分钟资金流它没有公开端点），但缺口全部可用东财公开接口补齐，同花顺依赖归零。

- `snapshot.py` → **腾讯 qt.gtimg.cn**：单接口批量（50/请求，实测 60 条稳定），
  股票+ETF 通用（免掉同花顺 fund 端点），字段全齐（现价/昨收/开/高/低/量手/
  额万/涨跌%/换手/量比），GBK 编码解析；`get_api_key()` 改为返回空串兼容旧调用。
- `evaluate.py` → **东财 RPT_LICO_FN_CPD**（营收同比 YSTZ/净利同比 SJLTZ/
  毛利率 XSMLL/ROE WEIGHTAVG_ROE）+ **RPT_DMSK_FN_BALANCE**（资产负债率=
  总负债/总资产）。无前视：按 `UPDATE_DATE <= 信号日` 取报告期最新行
  （**坑**：同一公告日返回多行新旧报告期，必须取 REPORTDATE 最大者；
  负债表排序列是 REPORT_DATE 不是 UPDATE_DATE，否则返回空）。
  评估并行化 6 线程（485 只串行 40 分钟 → 约 6-8 分钟）。
- `review.py` 涨停池：**当日**用东财 getTopicZTPool（连板 lbc/封单 fund/
  涨停时间 fbt/行业 hybk；**仅支持当日**）；**历史 12 日**用本地 zzshare 日线
  计算（close ≥ limit_price×0.998 判定 + 连板逐日累计，`_LOCAL_STREAK`）。
  东财无涨停原因关键词 → 题材分组改为按**行业**分组（页面已注明）。
- `review.py` 龙虎榜 → 东财 RPT_DAILYBILLBOARD_DETAILSNEW（买卖/净额/上榜
  原因；**机构/游资拆分东财没有**，卡片改"上榜净额/总买入/总卖出"，页面注明）。
- `stockpage.py` 日K 去掉同花顺兜底（只留腾讯 fqkline）；分时保留东财兜底。
- 部署/README/.env/CLI/GUI 文案全部更新：**MAINRISE_API_KEY 已不再需要**
  （旧 .api_key 文件可保留不影响）；`evaluate`/`snapshot` 命令帮助去掉 key 说明。
- 数据边界（不造假）：腾讯无全市场分钟资金流（东财 5 线保留）、无涨停池/龙虎榜
  公开端点、无详细财务公开端点（F10 controller 存在但路由未公开，勿依赖自选股
  westock CLI——闭源混淆）。

### 实时盯盘：秒级更新 + 分时缩略图

- `monitor.py` 盘中轮询 **30s → 3s**（腾讯快照单请求 0.1s，无压力）；页面内
  JS 每 3s 轮询 `/live.json` 原位更新 现价/涨跌%/今高/今低/KPI/提醒/市场状态，
  **不整页刷新**（meta 30s 兜底），排序状态不丢；live.json 保持轻量（不含缩略图
  序列，手机轮询不吃流量）。
- **分时缩略图**：live.html 表格新增「分时」列（SVG 92×30 红涨绿跌迷你折线，
  `data-min` 内嵌降采样点供 JS 续画）。数据来源三层：启动时一次性腾讯分时回填 →
  盘中每 3s 快照点累积（同秒覆盖）→ 预热线程每 5 分钟腾讯分时整日回填纠正；
  落盘 `output/state/live_minutes.json`（`stockpage.load_minutes/save_minutes/
  append_minute_point/backfill_minutes/minute_series`），服务重启不丢盘中数据。
- 实测：回填 36/36 只、缩略图 36 张、live_minutes.json 落盘；详情页 121K线+121
  分时点无错误；复盘当日 54 只涨停（东财）+ 近 12 日趋势全部本地计算。
- 测试：47 项全绿（新增 test_evaluate.py 2 项 + 涨停池解析/本地连板累计/龙虎榜
  映射/腾讯快照解析/分钟序列等；`test_review` 改 mock fetch_limit_up_pool 与
  fetch_dragon_tiger）。

### 部署实录（2026-08-13，腾讯云已同步）

- 同步方式：**rsync 直推**（本地 git 落后，origin 停在 8/07 仪表盘提交，monitor/
  review/stockpage 等从未提交；不要用 git pull 更新服务器）。已同步 mainrise/、
  tests/、deploy/、README、AGENTS、requirements、.env.example。
- 服务：`mainrise-monitor.service` 的 `--interval 30` 改为 **3**（daemon-reload +
  restart），日志确认"轮询 3s"；启动即回填 45/45 只分时缩略图。
- 实测：live.html 45 张缩略图 + 3s 轮询；复盘 2026-08-13 涨停 54 家、12 日趋势
  全真实；腾讯快照/东财财务在服务器可直接调用。公网 401 为 Basic Auth 正常。
- **权限修复（重要）**：cron 以 root 跑 daily_update.sh，产物归 root，导致
  ubuntu 的 monitor 服务刷新/`mainrise web` 报 PermissionError。已 `chown -R
  ubuntu:ubuntu output`，并在 daily_update.sh 末尾加
  `chown -R ubuntu:ubuntu "$APP_DIR/output"`（每次跑完自动归位，自愈）。
- Nginx 无需改动（live.json 静态托管）；`.api_key` 已不再需要（可留可删）。
- 本地 `dist/主升浪跟踪.app` 未含本轮改动，需要时另行打包。

## 会话记忆（2026-08-13，买点胜率改进落地）

研究结论（30051 条历史信号明细 + 近 3 日 16 个买点提示）：
- 模型现规则胜率 31.1%（2026 年 35.7%），靠止盈+8%/止损-5% 盈亏比盈利，中位 -5%。
- **防追高 80% 阈值是负优化**：10 日涨幅 30-80% 组胜率 36-39%、80%+ 组 40%，
  均显著优于 <30% 组（34.6%）。已把 `MAX_10D_GAIN` 80→150（tracker/cli/signals/
  web_dashboard 同步）。
- **买点2 企稳质量**：深回踩（日跌幅 >-3%）或收盘破 MA20 不算企稳，已加入
  `signals.row_status`（回踩低吸条件）。实测 08-11：原 7 提示→2 提示（恰好是
  两个赢家）；08-12：剔除 600801(-4.5%)/300191(-4.0%)。
- **无综合分不进买点提示区**：`buy_points` 过滤加 `(not has_scores) | notna(composite)`
  （观察池为空时保留新信号兜底）。**坑**：`~has_scores` 对 Python bool 是按位取反
  （~True=-2 恒真），必须用 `not`。08-12 验证：001337/000408/301219 已剔除。
- **板块退潮保护**（`tracker.weak_industries`）：同行业观察池 ≥3 只且均价 < 平均
  MA10 时暂停该行业买点（`INDUSTRY_KEYWORDS` 关键词映射，赛道→大行业）。
  **如实说明**：08-12 有色均价在 MA10 上方（56.67 vs 51.98），此规则对该案例不
  触发——板块级保护只拦截结构性破位，不拦"强势板块急跌"，后者靠回踩质量过滤。
- 大盘强弱（全A中位涨幅 MA5）、T+1 确认入场（跳空吃掉收益）经回测均无效，未实施。
- 测试：新增 6 项（企稳质量 3、防追高默认阈值、行业映射、板块退潮），合计 51 项全绿。

## 功能（2026-08-13，盯盘页「今日买入卖出信号」卡）

- live.html 标的行情卡**上方新增「今日买入卖出信号」卡**：只盯 **持仓票 + 待触发
  买点的票**（T0新信号/T1确认买点/回踩低吸/多头回踩，状态来自最新每日报告 CSV，
  非盘中 T0 扫描——用户明确不要盯当日 T0）。列：组/代码/名称/现价/涨跌%/实时量比/
  买入信号/卖出信号，随 3s 轮询动态增删改行。
- **卖出信号**（盘中动态）：持仓=止损-4%（低点）/高点回落8%/跌破MA10/跌破MA20；
  待买=破位（跌破 MA20）→ 放弃。注意：**待买票回踩到 MA10 是买点区，不显示
  "跌破MA10"**（该信号仅适用于持仓），避免自相矛盾。
- **买入信号**：持仓=持有中；T0新信号="今日确认→明日开盘买"（报告为昨日时）/
  "明日确认"（报告为今日时）；T1确认="✓ 今日/明日开盘买入"；回踩低吸/多头回踩=
  **14:30（BUY_EVAL_TIME）后评估**：回踩 MA10±1% 且缩量（实时量比<1）且未破
  MA20 → "✓ 企稳可买入（≈MA10）"。
- **实时量比**：腾讯快照成交量（手）×100 ÷（本地 zzshare 前 5 日均量/240×已交易
  分钟数）；`load_daily_features` 每日只读一次最近 25 个交易日文件（按日期缓存）。
- **盘中提醒只保留买卖信号**：删掉原 ±5% 涨跌幅、MA 触碰两条噪音规则
  （`CHG_ALERT/MA_TOUCH` 常量已删）；提醒=止损/高点回落/破位（卖出）+ 企稳可买入/
  买点1（买入）。
- 测试：52 项全绿（新增买卖信号、提醒规则重写）。注意 `~has_scores` 用 `not` 的坑。

## 修改（2026-08-13，T0 信号规则：涨停加量比门槛）

- `signals.py` 新增 `MIN_VR_LIMIT=1.0`：信号日 T0 = 多头 + 创20日新高 +
  （大阳线=涨幅≥5% 且量比≥1.5）或（**涨停且量比≥1.0**）。缩量涨停/一字板
  （量比<1）不再计信号；大阳线分支与原涨幅≥5% 等价（无新增信号）。
- 近 32 个交易日对比：旧 1067 → 新 977（-8.4%），**新增 0**，剔除 90 个缩量涨停
  （如 600721 量比0.1、001229 0.13、600683 0.17——大多是一字板/买不进）。
- **风华高科（000636）仍抓不到**：其涨停发生在均线"破位"期（不计信号），08-06
  均线修复后无涨停/量比≥1.5 的大阳线日——漏网根因是均线修复滞后 + 缩量爬升，
  不是量比门槛（之前结论不变）。
- 剔除名单含 000506 招金黄金（08-07 涨停量比0.92）等此前买点1 失败案例，新规则
  可避免该类缩量涨停追入。测试 54 项全绿（新增缩量涨停不计/放量涨停计 2 项）。

## 功能（2026-08-13，暴跌反转研究）

- 新增 `mainrise/reversal.py` + CLI `mainrise reversal`：规则=20 日内高点回撤
  ≥30%（近 10 日暴跌窗口）→ 首次放量涨停（量比≥1.0）且收盘仍低于 20 日高点 →
  次日收盘不破涨停日收盘（非连板）→ T+2 开盘买入。输出
  `output/reports/暴跌反转研究_*.md` + `暴跌反转信号_*.csv`（+3/+5/+10 日收益）。
  信号去重：一个暴跌周期只计一次（跳过至窗口结束）。
- **回测结果（2021-2026，1841 个有效信号）**：全历史 +5 日胜率 60.7%/均收
  +4.02%，+10 日 62.2%/+4.40%，显著优于主升浪基线（38.2%/-1.31%）。
- **必须如实说明的年份依赖**：2024 年 688 个信号胜率 77%（贡献主要超额）；
  2023/2025 年约 43-47% 无优势；**2026 年 +5 日 50%、精选组合仅 36.8%**，
  但 +10 日回到 63.6%/+4.2%——近年该模式需要更长持有（10 日）并容忍回撤，
  +5 日短线预期不成立。深回撤（≥30%）+ 量比 1-1.5 是最优子集（全历史 81%），
  近两年同样回落。个别信号 -30%+（如 603360 +10 日 -32%），必须配止损。
- 风华高科 07-29 被该模式抓到（07-31 入场，+5 日 +4.9%，后持续走高）。
- 测试：新增 tests/test_reversal.py 3 项（确认/不确认/缩量涨停），合计 57 项全绿。

## 修改（2026-08-13，模型范围限定为行业卡点企业）

- **原则（用户明确）**：模型追踪行业卡点企业，不是所有企业都适用。已把
  `load_chokepoint_codes()`（industry_info.csv 的 36 家代码集合）作为扫描范围：
  tracker 全市场新信号、backtest 回测、暴跌反转研究全部限定卡点企业
  （观察池/持仓逻辑不变，观察池由综合评分另行维护）。
- 暴跌反转研究改为**双范围对比**：全市场 1841 vs 卡点企业 21 个有效信号。
  卡点企业 +5 日胜率 61.9%/均收 +4.81%，**+10 日 71.4%/+10.01% 强于全市场**
  （62.2%/+4.40%）。子组（样本小，仅参考）：回撤 30-40% 85.7%、量比 1-1.5
  80%、**卡点强度 pos<85 的 71.4% 优于 pos≥85 的 42.9%**（核心卡点暴跌反转
  反而弱——核心企业暴跌多为基本面问题）。2024 年 7 个信号全赢。
- 风华高科已在报告注明"不在卡点名单，模型不追踪"。信号 CSV 拆分为
  `暴跌反转信号_卡点_*.csv` / `暴跌反转信号_全市场_*.csv`。
- 测试：新增 load_chokepoint_codes 断言，合计 58 项全绿。

## 修改（2026-08-13，盯盘股票列表 8 列排版）

「标的实时行情」表改为 8 列：①代码/名称合并（上名下码，代码可点进详情页）
②现价 ③缩略分时图 ④实时涨跌幅 ⑤实时量比（手×100÷5日均量分钟化）
⑥分组 ⑦买入信号 ⑧卖出信号（后两列为新加，原今高/今低/MA10/MA20/盘中提醒列
删除；盘中提醒区保留在卡片下方）。排序按钮索引同步改为涨跌3/现价1。

## 梳理（2026-08-13，行业卡点企业全景 v2）

- 产出 `docs/行业卡点企业全景.md`（8 大主题 × 全产业链分层梳理：AI硬件/国产
  半导体/存储/机器人/商业航天/自动驾驶/有色金属/创新药，每主题含产业链地图
  上游→中游→下游 + 环节×企业表 + 卡点逻辑（份额/壁垒/唯一性））与
  `docs/行业卡点企业_建议名单.csv`（36 家候选：9 家 S 级 + 27 家 A 级，
  pos 均"待评估"，未入 `industry_info.csv`）。两份文件已 rsync 到服务器
  `/home/ubuntu/chaoduan/docs/`。
- 现有名单 36 家覆盖对照：卡点相关 24 家（8 主题内），**非卡点 12 家**
  （证通电子/传智教育/宝通科技/潜能恒信/富祥股份/粤海饲料/科达制造/华新水泥/
  桂冠电力/天味食品/中际联合/东鹏饮料，原全市场高分池遗留）——与"模型只追踪
  卡点企业"定位不符，建议评估移出或另建观察组。
- 关键事实（2025-2026 研报）：中际旭创 800G 市占 40%+/1.6T 50%+（英伟达
  1.6T 采购份额约 80%）；澜起 DDR5 RCD 45%+（互连类 2025 全球第一 36.8%）；
  绿的谐波国内出货 27.5% 第一/人形机器人内 80-90%；锡业股份全球锡 27.16%/
  国内 53.35%（2005 年起全球第一）；德赛西威智驾域控第三方全球 8.8%/国内
  21.2%；药明康德全球 CXO 市占约 20-25%（TIDES 近 25%）；英维克冷板液冷
  国内第一约 40-53%。
- 未上市/港股卡点（不入 A 股名单，只做产业跟踪）：长鑫存储、长江存储、
  地平线、禾赛、速腾聚创、优必选。
- **待用户确认**：是否将 S/A 级候选并入 `industry_info.csv`（并入会改变
  backtest/reversal/report 追踪范围，需先跑 pos 评分复核）。

## 修改（2026-08-13，卡点名单 36 → 72）

- **用户确认：S/A 级候选全部并入，非卡点 12 家保留。**
  `mainrise/resources/industry_info.csv` 由 36 家扩至 72 家（新增光模块/PCB/液冷、
  半导体设备/封测/代工、内存接口/模组、减速器/丝杠/伺服、星载射频/测运控、
  智驾域控/线控、锡/稀土/钴镍、CXO/创新药出海等 36 家）。
- 36 家新增标的均已用东财已披露报告期（2026Q1/中报，无前视）复核财务，
  pos 初评按"产业链地位+财务质量"给出（S 级 90-95、A 级 78-90），弱财务
  （英维克 2026Q1 净利-82%、韦尔-42%、德赛西威-21%、汇川-23%、柯力-46%、
  兆威-25%、中科星图 Q1 营收-60% 亏损）已写入 risk 字段，可再手工微调。
- `docs/行业卡点企业全景.md` 同步更新（⭐ 覆盖 72 家、覆盖对照 60+12、
  第 10 节改"已并入名单"）；`docs/行业卡点企业_建议名单.csv` pos 已回填。
- 测试 58 项全绿（chokepoint 断言仅要求 ≥30 且不含风华高科，不受影响）。
- 已 rsync `industry_info.csv` + `docs/` 到服务器；下次 backtest/reversal/
  report/track 自动按 72 家范围运行（B 级候选 24 家暂未并入，按需再补）。

## 研究（2026-08-13，72 只卡点标的走势与买卖点）

- 按新 72 只范围跑完整流水线：backtest（595 信号/胜率 42.0%/均收 +1.11%/
  PF 1.457）→ evaluate（40 日 47 只有信号，重点线索 35 只）→ report（观察池
  35 只）→ track（2 活跃持仓：厦门钨业/中际联合 08-12 买入；6 只买点2：
  紫金矿业/厦门钨业/中际联合/金诚信/中金黄金/富祥股份）→ reversal（卡点
  +5 日胜率 63.2%、均收 +3.58%；近期反转：深科技/长电 07-21 +5 日收跌，
  中钨/翔鹭/沪电/中瓷/长飞 08-04~05，粤海 08-06）。
- 产出 `docs/72只卡点标的走势与买卖点研究.md`：主题分布、强势多头 32 只/
  回踩低吸 6 只/破位 11 只/空头 23 只，逐主题速查表（MA5/10/20、10/20/60 日
  涨幅、高点回撤、综合分、买卖点提示），操作纪律 9 条。行情基准 2026-08-12。
- 主题统计口径：8 主题 60 只 + 遗留非卡点 12 只单列"其他"；关键词归类已修
  （中钨"PCB微钻"不再误入 AI 硬件、天孚"光引擎"归 AI 硬件）。
- **修复 backtest 空表崩溃**：72 只小范围下某些参数组扫描结果为空，空 DataFrame
  无 `S_date` 列，原"先过滤后判空"触发 KeyError；改为先判空再过滤（全市场
  大样本不会触发，属新范围暴露的问题）。
- 测试 58 项全绿；研究文档与 backtest.py 修复已 rsync 服务器。

## 功能（2026-08-13，起涨特征研究 + 全市场回测）

- 新增 `mainrise/launch.py` + CLI `mainrise launch`（约 85 秒）：三步研究
  ①提取 72 只卡点票历史起涨日（未来20日≥20%且前期20日≤10%的段落内涨幅最大
  一天，仅画像用）②量化起涨特征并对比全市场 ③无未来函数全市场回测
  （次日开盘买入、5/10/20日收盘卖出、双边费0.2%），输出
  `output/reports/起涨特征研究_<date>.md` + `起涨信号_全市场_<date>.csv`
  （r7m+r9 历史信号明细），并归档 `docs/72只起涨特征与全市场回测.md`。
- **72 只起涨画像**（1074 段）：前期20日回撤中位 -12%、启动前缩量阴跌、当日
  放量1.4倍、涨幅中位+5.1%、涨停占比16%、创20日新高仅22.5%、前日多头排列仅
  21% → 卡点票起涨以"深蹲后放量启动"为主，不是追高突破；与全市场同口径几乎
  一致 → 特征可全市场量化。
- **特征预测力**（全市场 2.2 万起涨日分桶）：回撤越深越好（<-20%档 fwd10 +9.9%）、
  量比与后续涨幅**负相关**（>2.1 倍巨量最差）、市场涨停家数 ≥130 档最优
  （fwd10 +9.6%）、启动前缩量阴跌优于已预热。
- **全市场规则回测**（无未来函数）：
  - r4 超跌启动（回撤≥15%+涨≥5%+量比≥1.3）：n=5.9万，5日胜率49.8%、PF 1.38
  - r7m（r4+温和量比1~2+全市场涨停≥90）：n=3.2万，5日胜率63.4%、均收+3.93%、
    PF 2.71；20日 P(≥10%)=41.6%
  - r9（r4+涨停≥130）：n=1.1万，5日胜率71.1%、均收+5.97%、PF 4.90
  - 基准全市场任意日 5日胜率46.5%/PF 1.04；**原 T0 裸跑全市场 5日胜率仅36.8%/
    PF 0.72**（未加防追高/板块过滤，仅供同口径对比）
  - 逐年：r7m 2024 最强（71%/+6.1%），2026 年 5日仍 54%/+2.0% 但 10日衰减
    （48%/+0.1%）→ 今年超跌反弹兑现快，宜短线。
- 近期卡点名单内 r7m 信号：08-05 胜宏科技/中微/富创/拓荆/江丰/长飞/中钨；
  08-10 奥普光电/富创精密/江波龙/东鹏饮料。
- 测试：新增 tests/test_launch.py 3 项（段落检测/信号掩码/市场门限），合计 61 项全绿。

## 深挖（2026-08-13，r7m/r9 信号日之前特征量化）

- `launch.py` 新增 20 个前置特征（pre3/5/10、跳空、实体、收盘位置、上影线、振幅、
  低开高走、站上 MA5/10/20、创新低反包、前一日涨跌/量能、5日量/20日量、20日回撤、
  距高点天数、日均回撤速度），全部无未来函数；报告新增第六/七节，运行时约 2.2 分钟。
- **前置画像**（r7m/r9 信号日之前，中位数）：前10日仍深跌 -5%、前3日已转涨 +4.5%
  （多为反弹第2-3天）、前一日仍阴跌 -1.2%、当日收盘在振幅 90% 位置、上影线仅 10%、
  距20日高点 17 个交易日缓跌（-1.75%/日）、20日回撤中位 -24%、站上MA5 占比 81%、
  创新低反包占比 12-18%（基准仅 3%）。
- **增强规则回测**（无未来函数）：
  - r7mA = r7m + 前5日涨幅<0（干净超跌首阳）：n=1.5万，5日胜率 71.3%/+5.95%、
    PF 4.27（r7m 从 63.4%/2.71 提升）
  - r9A = r9 + 前5日<0：n=5.2千，5日胜率 83.0%/+8.86%、PF 14.86
  - r7mE = r7m + 创新20日低点当日反包：n=4千，5日 71.8%、20日胜率 82.3%/+17.1%
  - r9E = r9 + 创新低反包：n=2千，5日 85.1%/+10.8%、PF 20.9（超激进，样本少）
  - 无增益项：收盘位置≥0.6、前期缩量（vol5_v20≤1.0）反而略降 → 判断"干净启动"
    用 pre5<0 比量能更有效。
- **逐年警示**：增强规则 2024 最强（r9A 5日 91%/+11.9%），**2026 年大幅衰减**
  （r7mA 45%/+1.2%、r7mE 29%/-3.2%、r9E 33%/-4.2%）→ 2026 超跌首阳短线
  兑现快，增强规则当前只做加分项，不单独重仓。
- 结论：前置特征可量化；实战优先级 r7mA > r9A > r7mE/r9E；CSV 明细含
  r7m/r9/r7mA/r7mE/r9A/r9E 六档信号与前置特征列。
- 测试：test_launch.py 扩至 4 项（新增前置特征+增强掩码），合计 62 项全绿。

## 功能（2026-08-13，启动加仓投资模型）

- 新增 `mainrise/strategy.py` + CLI `mainrise strategy`（约 1 分钟）：把
  "前置特征打底仓 → 次日判定加仓 → 止盈止损" 固化为可回测模型，输出
  `output/reports/启动加仓模型_<date>.md` + `_交易明细_<date>.csv`（20,213 笔），
  模型说明书 `docs/启动加仓投资模型.md`。
- **模型规则**：信号=r7mA（回撤≥15%+涨≥5%+量比1~2+前5日<0+涨停≥90）或 r9A
  （涨停≥130）；T+1 开盘打底仓（计划仓 2/3）→ T+1 收盘站上 MA10 → T+2 开盘加仓
  （1/3，底:加=2:1）；止损=加权均价-7%（盘中低点）；止盈=最高价回落10%（收盘判定）；
  时间止损=10 日；**市场退潮保护=持仓期涨停家数<50 次日清仓**。
- **回测（无未来函数/费用0.1%每笔）**：r9A 完整模型 n=5,197，胜率 77.2%、
  均收 +10.65%、PF 10.43；r7mA 胜率 64.0%、PF 5.05。仅底仓版略优
  （r9A 77.9%/+11.17%/PF 11.05）→ 加仓是仓位放大器不是收益来源。
- **参数发现**：-7%/回落10% 优于 -5%/8%（胜率 68→77%、PF 6.05→7.65，
  超跌票波动大、窄止损被扫）；退潮保护把 2024 年均收 +8.4%→+13.3%，
  2026 年由负转正（38%/+0.6%），66% 交易由此退出。
- **逐年**：2022/2024/2025 强（81%/77%/83%）；2026 衰减（37%/-0.7%）→
  建议半仓+只做强市场信号。单票≤1/3、并行≤3 只。
- 数据边界：2026 年样本弱；止盈=利润回吐保护不保证盈利；未到期持仓
  （数据截断）标注"未平仓"不计入统计。
- 测试：新增 tests/test_strategy.py 4 项（首日止损/加仓止盈/退潮退出/不满足
  MA10 不加仓），合计 66 项全绿。

## 接入（2026-08-13，启动模型接入盯盘页 + 每日报告）

- **monitor.py**：
  - `load_monitor_rows` 增加 72 只卡点企业扫描范围（`scan_codes`，不与主表重复）；
    `load_daily_features` 新增 close5/close10（前5日涨幅判定用）。
  - 新增 `_launch_signal`（r7mA/r9A 盘中判定：回撤≥15% + 涨≥5% + 前5日<0 +
    市场门限，r9A 量比≥1.3/涨停≥130，r7mA 量比1~2/涨停≥90）、
    `_realtime_zt_count`（东财涨停池 60s 缓存，失败返回 None 不误判）、
    `_volume_ratio` 抽取复用。
  - `build_state` 支持 mkt_zt/scan_codes：卡点名单仅在有启动信号时才进入
    标的列表（group=启动，in_card=True，弹出"📈 启动信号：明日开盘打底仓"提醒）；
    市场退潮保护：全市场涨停<50 全局提醒一次 + 持仓卖出列提示"退潮保护"。
  - live.html 新增常驻"启动加仓模型（盘中）"卡片（市场涨停家数 +
    退潮红色警告 + 信号列表）。
  - 退出场景：东财涨停池失败时市场门限不判定（不误触发/误清仓）。
- **tracker.py**：每日报告新增第二节"启动加仓模型（明日打底仓候选）"
  （卡点范围 r7mA/r9A，收盘判定，动作=明日开盘打底仓）；原章节顺延。
- 本地验证：monitor --once 正常（35 只 + 启动卡片渲染）；64 项测试全绿
  （test_monitor 新增 2 项：信号阈值、扫描行+卡片渲染）。

## 修改（2026-08-13，卡点名单 72 → 93：新增 5 方向）

- 用户要求新增 5 个卡点方向，`industry_info.csv` 由 72 家扩至 **93 家**：
  - **MLCC**：三环集团(300408,pos90)、风华高科(000636,pos88)、鸿远电子(603267,85)、
    洁美科技(002859,82)
  - **金刚石散热**：中兵红箭(000519,88)、黄河旋风(600172,85)、力量钻石(301071,85)、
    国机精工(002046,82)
  - **玻璃基板**：沃格光电(603773,88)、菲利华(300395,90)、彩虹股份(600707,85)、
    凯盛科技(600552,82)
  - **算电协同**：中恒电气(002364,85)、科华数据(002335,85)、麦格米特(002851,88)、
    欧陆通(300870,82)
  - **算力租赁**：润泽科技(300442,90)、数据港(603881,85)、奥飞数据(300738,80)、
    中贝通信(603220,82)、云赛智联(600602,82)
- **风华高科正式入名单**（MLCC 卡点，用户此前关注过的散户强势票）；
  `reversal.py` 的"风华高科是否在名单"提示自动翻转为"在名单，模型正常追踪"；
  `tests/test_reversal.py` 断言改为"600519 贵州茅台不在卡点名单"。
- `docs/行业卡点企业全景.md` 更新：覆盖对照 60→81 卡点相关（+21）、新增第 9 节
  五个方向明细、章节顺延；追踪范围口径改为 93 家。
- 验证：load_chokepoint_codes=93、64 项测试全绿；已同步服务器
  （industry_info.csv / docs / tests / AGENTS.md），下一轮 backtest/reversal/
  track/monitor 自动按 93 家运行。

## 检查（2026-08-13，十五五"六张网"覆盖）

- 口径：十五五"六张网" = **水网、新型电网、算力网、新一代通信网、城市地下管网、
  物流网**（2026 发改委两会提出，政治局/国常会点名，年投资估算超 7 万亿）。
- 产出 `docs/六张网覆盖检查.md` + `docs/六张网缺口候选.csv`（29 家候选，
  pos 待评估，未并入名单）。
- **覆盖结论（93 家对照）**：
  - ✅ 算力网：约 50 家，覆盖最完整（光模块/PCB/液冷/服务器/存储/半导体/算力租赁/
    算电协同/MLCC/金刚石散热/玻璃基板）
  - 🟡 新一代通信网：部分（长飞光纤 + 商业航天 4 家），缺地面主设备/海缆/模组
    （中兴/烽火/紫光/中天/亨通/移远/海格）
  - 🟡 新型电网：部分（算电协同 4 家 + 桂冠水电 + 有色上游），缺特高压/输配电/
    充电（特变电工/许继/国电南瑞/思源/特锐德/阳光电源）
  - 🔴 水网：空白（候选：中国电建/三峡水利/利欧/新兴铸管/大禹节水）
  - 🔴 城市地下管网：空白（候选：新兴铸管/伟星新材/公元/青龙/东宏/金洲）
  - 🔴 物流网：空白（候选：顺丰/中远海控/传化/兰剑/今天国际/嘉友）
- 模型目前偏"算力+"单极；传统基建三张网完全空白。**待用户确认**是否把 29 家
  缺口候选并入 industry_info.csv（并入后名单 93→约 122，影响回测/反转/盯盘范围）。

## 修改（2026-08-13，六张网缺口 29 家并入，名单 93 → 122）

- **用户确认：29 家缺口候选全部并入** `industry_info.csv`（名单 93 → **122 家**）。
- 已并入：水网（中国电建/三峡水利/利欧股份/大禹节水）、水网+管网双卡位
  （新兴铸管）、管网（伟星新材/公元/东宏/青龙/金洲）、物流网（顺丰/中远海控/
  嘉友国际/传化智联/兰剑智能/今天国际）、新型电网（国电南瑞/特变电工/阳光电源/
  思源电气/许继电气/特锐德）、新一代通信网（中兴/紫光股份/烽火/亨通/中天/移远/
  海格通信）。
- 29 家均用东财已披露报告期（2026Q1/中报，无前视）复核财务，pos 初评
  （顺丰/中兴/国电南瑞 92；新兴铸管/伟星 88；中远海控/特变/阳光/思源 85-90；
  弱财务如利欧 Q1净利-71%、青龙双降、海格-87% 已写入 risk）。
- `docs/六张网缺口候选.csv` pos 已回填；`docs/行业卡点企业全景.md` 新增 9A 节
  （六张网 29 家明细）、覆盖对照更新 110+12=122。
- 验证：load_chokepoint_codes=122、64 项测试全绿；已同步服务器，下一轮
  backtest/reversal/track/monitor 自动按 122 家运行。

## 文档（2026-08-13，项目经验）

- 新增 `docs/项目经验.md`：简历/作品集向项目经验（角色/周期/状态、六大模块
  工作与量化成果、难点与解决方案、技术栈、一句话总结），量化数字全部取自
  真实回测（r9A 胜率 77.2%/PF 10.47、暴跌反转 +5日 63.2%、64 项测试等）。

## 文档（2026-08-13，项目维护手册）

- 新增 `docs/项目维护手册.md`：**换电脑/接手维护先读**——10 节全量细节：
  三句话看懂项目 / 核心模型规则（T0、买点纪律、综合评分、启动加仓模型、
  暴跌反转、起涨研究）/ 数据与免费接口（腾讯+东财、无前视、缓存策略）/
  模块清单与关键路径 / 命令与流水线 / 卡点名单管理 / 部署（rsync 同步、
  换机恢复流程）/ 已知坑与决策记录 / 测试体系 / 研究结论速查 / 维护动作速查。

## 研究（2026-08-13，启动后 T0 与主升浪）

- 问题：启动加仓模型买点后再触发 T0，是否进入主升浪？口径：T0 信号日后
  **半年（120 交易日）区间最大涨幅**，全市场 + 卡点双范围，无未来函数。
- **结论：不会。** 全市场"启动→T0 序列"n=5,284，半年最大涨幅中位 +23%、
  P(≥40%)=29.8%；裸 T0 n=77,497 中位 +26%、P(≥40%)=35.6%——前置启动
  **不提升 T0 质量，反而略降**。
- 真正决定主升浪的：①T0 本身（卡点名单裸 T0：中位 +32%、P(≥40%)=43.7%，
  显著强于全市场，卡点筛选有价值）；②**T0 触发时启动以来累计涨幅**——
  <5% 未透支：中位 +29%、P(≥40%)=38.6%、见顶 39 日；≥30% 已透支：中位
  +20%、P(≥40%)=26.4%、见顶 20 日（鱼尾）。
- 实操：启动模型持仓中再出 T0 **不加仓追高**；T0 时已透支 ≥30% 视为减仓/
  兑现信号；卡点名单内裸 T0 可作趋势确认加分项。
- 产出 `docs/启动后T0与主升浪研究.md` + `output/reports/启动T0序列研究_2026-08-13.md`。

## 修改（2026-08-13，追踪范围纯卡点 + T0 底部涨幅加减分）

- **追踪范围收紧为纯卡点名单（110 家）**：`industry_info.csv` 移除 12 家
  非卡点遗留（证通/传智/宝通/潜能恒信/富祥/粤海/科达/华新水泥/桂冠/天味/
  中际联合/东鹏）。持仓仍按"持仓"组盯盘（不影响已持有标的退出管理）；
  重跑全流水线后观察池 35 → 26 只，全部在名单内（富祥股份等不再进买点区）。
- **T0 底部涨幅加减分**（tracker + monitor）：
  - 底部涨幅 = T0 信号日收盘 / 信号日及此前 60 日最低价 - 1
    （`tracker.bottom_gain()`，模块级可测；`T0_BONUS=15` / `T0_OVERBOUGHT=30`）。
  - **<15% 主升确认加分**（状态标"主升加分"）；15-30% 中性；
    **≥30% 透支 → 减仓信号，不是买点**（不进买点区，报告单列
    "T0 透支减仓提示"，第四节新信号标"T0透支→减仓不追"）。
  - 盯盘页：T0 新信号/T1 确认按 lo60（日线特征新增 lo60 列）实时算底部涨幅，
    透支显示"T0透支…减仓/不追"并弹提醒；加分显示"T0主升确认…加分买点"。
- 测试：新增 tests/test_tracker.py（bottom_gain 三档）、test_monitor.py
  T0 加减分用例；**当前套件实为 64 项全绿**（此前记录的 68 有误，已统一修正）。
- 已同步服务器（industry_info/tracker/monitor/tests/docs/AGENTS），
  monitor 服务需重启生效。

## 功能（2026-08-13，K线接入 KLineChart 看盘套件）

- 用户要求"找 GitHub 现成看盘套件挂上去"，评估 4 个候选后按推荐接入
  **KLineChart v10.0.2**（Apache-2.0，单文件 234KB，本地内联无 CDN）：
  `mainrise/resources/klinecharts.min.js`；评估文档 `docs/看盘套件评估.md`。
- `mainrise/stockpage.py`：详情页 K线 由手绘 SVG 改为 KLineChart canvas 渲染。
  新 `PAGE_JS` 保留旧 `PAGE_JS_LEGACY`（SVG）作为资源缺失时的降级，不白屏。
  **v10 关键 API（与 v9 不兼容）**：`init → setSymbol → setPeriod →
  setDataLoader({getBars})`，数据回调 `callback(bars,{forward:false,backward:false})`；
  MA 要叠主图必须显式 `paneId:'candle_pane'`（v10 默认另开 pane）；VOL 直接
  `createIndicator('VOL')` 自动开副图；信号箭头用 `simpleAnnotation` 覆盖物
  （points 传单点 + extendData 文本）；线段用 `segment`（2 点，传全即完成绘制）。
- 保留的全部功能：MA5/10/20/60（黄/蓝/紫/绿）、VOL 量能、回归通道(20实/60虚)、
  支撑/压力连线、T0/T1/T2 红箭头、平仓绿箭头、十字光标 OHLC 提示、红涨绿跌、
  手机自适应；默认近 60 根，放大/缩小/全部/更早/更近按钮 + 滚轮/触摸缩放平移
  （`setBarSpace(width/cnt)` + `scrollToDataIndex(end-1)`，`onVisibleRangeChange`
  同步"近 N 日"标签）；`initKChart` try/catch 出错显示提示不白屏。
- 打包：两个构建脚本已加 `klinecharts.min.js` add-data。
- 测试：`tests/test_stockpage.py` 断言套件内联 + 降级用例，**65 项全绿**。
- 服务器已同步 stockpage.py + klinecharts.min.js 并重启 mainrise-monitor；
  详情页 127.0.0.1:8765 200（254KB），公网经 Nginx+Basic Auth 正常。
- **坑（已修）**：内联库首次部署报"klinecharts is not defined"——`STOCK_PAGE`
  模板已带 `<script>{klc}</script>` 外壳，若再给 klc 内容包一层 `<script>` 会
  双包标签，浏览器把内层第一个 `</script>` 当脚本结束，库以语法错误被丢弃。
  修复：`stock_page_html` 只传库原始 JS；测试已加
  `assertNotIn("<script><script>", html)` 防回归。浏览器端需强制刷新（Cmd+Shift+R）
  拿新页面。
- 待办：用户浏览器端目测确认渲染/缩放/标注无异常（本机无 Node/无头浏览器，
  仅做 HTTP 冒烟 + JS 括号平衡校验；出错会显示"K线组件初始化失败"便于定位）。

## 研究（2026-08-13，回测成功率 vs 全市场成交量）

- 用户问"下午回测数据里，成功率是否和全市场成交量有关"。新增可复用脚本
  `scripts/backtest_market_volume.py`（口径与 backtest.py 完全一致：开盘买入、
  止损-5%、峰值回落8%、5日时间止损、成本0.2%），输出
  `docs/回测与全市场成交量.md`。
- 全市场成交额/成交量 = `data/zzshare_daily/*.csv` 逐日 sum(amount)/sum(volume)；
  **量能水位 = 当日全市场成交额 / 其20日均值**（剔除新股扩容趋势）。
- 样本：914 笔（卡点名单，2022-01 ~ 2026-08，v3 纪律），总口径
  胜率44.1% / 均收+1.76% / PF 1.75。
- **核心发现（倒U型，非线性）**：
  - 温和放量 1.10~1.30 最优（胜率50.4%、PF 2.19；其中 1.20~1.30 达 57.5%、PF 2.67）；
  - 地量(<0.90) 与常态差别不大（44~45%）；
  - **天量 ≥1.50 是断崖**：93 笔里 88 笔在 2024（胜率14.8% vs 非放量42.5%，
    即 2024 年 9-10 月天量追高），整体胜率 17.2%、均收 -1.75%、PF 0.57；
  - **2026 年反例**：>1.30 放量日胜率 62.5%（强趋势中放量=增量入场），
    所以不能硬性"放量就砍"；
  - 线性相关系数为负（成交额水位 vs 胜负 -0.17），但主要由 Q5 极端尾部拖累；
  - 市场宽度（站上MA20占比≥50%）无法区分好坏（2024 放量日宽度也高但胜率低）。
- **建议规则（未改模型，待用户确认）**：① 信号日成交额水位 ≥1.50 直接剔除
  （过滤后 821 笔：胜率 47.1%、均收 +2.16%、PF 2.01）；② 1.10~1.30 加分；
  ③ 强趋势年份不另惩罚放量日。可在 backtest.py 加该过滤参数全样本验证。
- 已同步服务器 scripts/ 与 docs/（无需重启服务）。

## 研究（2026-08-13，启动加仓模型 vs 全市场成交量）

- 用户追问"成交额水位分析针对 T0 还是启动加仓模型"→ 确认是 **T0 模型**
  （mainrise_trades.csv 由 backtest.py 用 T0 规则生成）；随后对
  **启动加仓模型**（`output/reports/启动加仓模型_交易明细_2026-08-13.csv`，
  20,213 笔：r7mA 15,007 + r9A 5,206，全市场，ret 为策略输出含退潮保护）
  跑了同一套成交额水位分析，`scripts/backtest_market_volume.py --strategy` 生成
  `docs/启动加仓模型与全市场成交量.md`。
- **结论与 T0 相反：启动加仓模型量能越高越好（正相关）**：
  - 成交额水位与收益 Pearson +0.41 / Spearman +0.50，与胜负 +0.32；
    水位五分位单调上升（Q1 37.8% → Q5 93.5%）；
  - 温和放量 1.10~1.30 胜率 82.0%、均收 +13.24%、PF 15.93（最优）；
  - **低量是坑**：水位 <1.00 胜率 34~38% 均收为负（0.90~1.00 最差 34.1%；
    2024 低水位 2865 笔仅 37.3%）——缩量涨停潮无增量资金，共振反弹走不远；
  - 过滤：剔除低量(<1.00) → 14,737 笔，胜率 78.3%、+11.17%、PF 11.28
    （vs 全部 68.0%/8.26%/6.27）；
  - 天量 ≥1.50 样本仅 30 笔，不构成问题（与 T0 的 2024 天量断崖相反）。
- **2026 年衰减是环境问题，量能过滤救不了**：2026 低水位 44.3% vs 高水位
  40.4% 都差（>1.30 的 53 笔 52.8% 稍好但样本小）；与 T0 模型 2026 放量日
  反而好（62.5%）形成对比 → 两套模型 2026 都应降档/半仓运行。
- 机制解读：T0 赚个股趋势突破的钱，天量日易借情绪出货（倒U）；
  启动加仓赚市场共振反弹的钱，需要增量资金确认（正相关）。
- 建议（未改模型）：启动加仓模型信号日成交额水位 ≥1.00（最好 ≥1.10）
  作为前置条件；T0 模型 ≥1.50 剔除。两套模型各自验证后可进 backtest.py/strategy.py。
- 已同步服务器 scripts/ 与 docs/（无需重启服务）。

## 研究（2026-08-13，剔除 7-8 月后 2026 表现）

- 启动加仓模型：**剔除 7-8 月后 2026 反而更差**——1-6 月 36.9% 胜率 / +0.32% /
  PF 1.09（1 月 20.8%、2 月 9.1%、6 月 33.1% 都很差），7 月 41.6%/+2.52%/PF 1.94
  略好于上半年 → "2026 衰减"是**全年性环境问题**，不是最近一个半月崩出来的。
- **8 月 96.6% 胜率是回测尾部假象**：148 笔里 136 笔"未平仓"（回测数据只到
  8/10，10 日持仓窗口未走完），不可信；回看月度统计时必须过滤未平仓。
- T0 模型：1-6 月 57.1% / +4.57% / PF 3.47，7-8 月 61.1%（n=18）→
  **"2026 衰减"只适用于启动加仓模型，T0 今年反而是近年最佳之一**
  （对比 2022 30.0%、2024 32.1%）。两套模型今年分化：趋势突破正常偏强，
  超跌反弹失灵。
- 已同步服务器 AGENTS.md。

## 研究（2026-08-13，双模型轮动验证）

- 用户提出"杀跌后做启动加仓、趋势形成后做主升浪"，用两套模型全样本按
  信号日大盘状态交叉验证，报告 `docs/双模型轮动研究.md`：
  - **大盘状态口径**：全市场等权 20 日累计涨幅 mkt_ret20（zzshare pct_chg
    rolling20 sum）+ 市场宽度（站上MA20占比）+ 成交额水位。
  - **启动加仓 × mkt_ret20（修正口径）**：杀跌(≤-5%) 74.6%/PF 9.42；
    主升(>+5%) 40.7%/PF 1.20 → "杀跌后做启动加仓"成立，市场已涨超5%时停。
  - **启动加仓 × 状态 × 量能**：杀跌+量能1.0~1.3 86.2%/PF 20.74；非主升
    (≤+5%)+量能≥1.0 80.0%/PF 12.77；缩量(<1.0)是坑 37.8%。
  - **⚠ 数据缺陷已修**：等权均值会被新股/复牌脏数据（prev_close=0 →
    pct_chg=+50000%级）虚增——实测 2026-08-11 三只脏股把当日均值从 ~0.5%
    拉成 +11.4%，20 日累计从 +4.6% 虚增到 +29.3%。**修正口径：剔除
    prev_close≤0 + 每日涨幅裁剪 ±21%**（`market_state.py` 已实现）。
- **2026 真相（修正后）**：此前"84% 信号在主升区、门禁后胜率回 72%"是脏数据
  假象；修正后 2026 年**每个状态都失效**（杀跌34.7%/弱势32.7%/强势29.8%/
  主升39.6%），三态门禁+量能过滤救不了 2026（非主升+量能≥1.0 仅 38.4%）。
- **结构性错配解释（用户质疑后补证）**：2026-04~06 是科技结构性牛市——科技卡点
  等权 +32.7%、科创板 +21.0%、非科技 -7.6%、宽度中位数 33%。超跌反弹过滤器在
  这种行情里 98% 信号落在非科技跌货上（998/1022 笔，37.6% 胜率），少数科技信号
  54.2%/PF 2.35（如 600707 +51%）；同期 T0 模型 66.3%/PF 5.32 正常（范围就是
  科技卡点）。**启动加仓实盘只扫卡点（live 口径 24 笔 54.2% 未失效），回测全市场
  口径低估了模型**。详见 docs/双模型轮动研究.md 三b。
- **结构强弱差验证（用户提出"科技强推科技/非科技强推非科技"）**：tech vs other
  等权 20 日涨幅差是全历史强环境信号——科技强(diff≥+5pp)+量能≥1.0 非2026 年份
  胜率 91.7%/PF 40.8（黄金区）；非科技强(diff≤-5pp)是最差环境（40.9%）；但
  **"推强结构"不能救 2026**（2026 科技强区间 909 笔仅 37.3%，含量能≥1.0），
  因为 2026 信号集中在 5-6 月科技高潮+7 月顶部崩溃，且超跌模型在强势结构里
  触发极少（科技信号仅 17-27 笔）。正确适应方式是**模型层切换**：科技强→T0
  主升浪为主；非科技强→环境偏弱双模型降档；均衡→双模型正常。
  - **T0 × 大盘状态**：震荡区最好（47.9%/PF 2.04），主升区衰减（39.1%/
    PF 1.50）→ "趋势形成后做主升浪"只对一半，主升后段两套模型都降档。
- **三态轮动规则（已上线大盘状态卡）**：杀跌区(≤-5%)只开启动加仓（+量能≥1.0）；
  震荡区(-5~+5%)双模型并行；主升区(>+5%或宽度≥70%)启动加仓停、T0 降档。
- 实施待办：strategy.py 加 --max-mkt-ret20/--min-amount-wl 过滤参数验证；
  实盘口径可参考上证指数 20 日涨幅（等权与指数口径略异，阈值 -5%/+5% 起步）。

## 功能（2026-08-13，大盘状态卡已上线）

- 新增 `mainrise/market_state.py`：三态判定（杀跌≤-5% / 震荡±5% / 主升>+5% 或
  宽度≥70%）+ 模型建议；`compute_daily`（修正口径，写 output/state/
  market_state.json + history.csv，track 每日流水线自动调用）；
  `live_state` 盘中刷新上证指数 20 日涨幅（腾讯 sh000001 日K，5 分钟缓存）；
  `render_card` 生成盯盘页卡片。
- 接入：`mainrise track` 报告新增"大盘状态（三态轮动）"节（主升区时启动加仓
  候选加"应停开"警告）；盯盘页"盘中提醒"卡后新增大盘状态卡（三态红黄绿 +
  等权20日/上证20日/宽度/量能 + 建议），每轮 3s 自动更新（上证指数 5 分钟缓存）；
  CLI 新增 `mainrise market-state`。
- 2026-08-12 实测：等权20日 +5.4%、宽度 89%、量能 0.91 → 主升区（宽度触发），
  建议"启动加仓停开 ｜ T0 降档防追高 ｜ ⚠量能不足"。
- 测试：tests/test_market_state.py 6 项（三态/量能警告/落盘/脏数据剔除/卡片/
  上证缓存），**71 项全绿**。
- **已部署**：服务器 venv 生成 2026-08-13 状态（主升区 +4.8%/宽度85%/量能1.08），
  live.html 大盘状态卡已上线（`monitor --once` 强制生成），重启服务生效；
  每日 17:30 流水线（track）自动更新大盘状态 + 报告节。
- **坑（已修）**：`monitor.load_daily_features` 的 usecols 漏了 `"low"` 却算
  lo60（T0 底部涨幅加减分引入），任何一轮盯盘都会 KeyError 崩溃——服务器
  收盘后重启所以没当场暴露，次日 9:30 首轮必崩。已补 `"low"` + 回归测试
  `test_load_daily_features_includes_low`（tests/test_monitor.py）。
- **结构维度已进大盘状态卡**：`market_state.compute_daily` 新增 tech20/other20/
  diff（科技卡点 vs 非科技 等权20日涨幅差）+ structure 三档（科技强≥+5pp/
  均衡/非科技强≤-5pp）+ 自适应建议（科技强→T0 主升浪为主；非科技强→降档；
  均衡→双模型正常）；盯盘卡新增"科技20日/非科技20日/强弱差"行，每日报告
  新增结构行。2026-08-12 实测：科技+0.46% vs 非科技+5.51%，diff -5.05pp →
  非科技强（7 月科技大跌后非科技补涨）。测试 +2 项（structure 三档/卡片），
  **73 项全绿**。

## 功能（2026-08-13，买入信号按市场状态自适应）

- 用户要求"买入信号也要按市场调整策略"，已实现自适应逻辑（大盘状态卡是
  数据源，买入信号是执行端）：
  - **启动加仓动作自适应**（`market_state.launch_action`）：主升区→"仅观察
    （主升区停开）"，不弹"打底仓"提醒；量能<1.0→"量能不足等放量"；非科技强→
    "环境偏弱轻仓"；杀跌/震荡+量能足→"明日开盘打底仓"。盯盘启动卡 动作 列、
    盘中提醒、每日报告候选表三处统一。
  - **T0 买点前缀**（`market_state.t0_note`）：主升区→"⚠主升区·轻仓"；科技强→
    "科技强·优先"；非科技强→"⚠非科技强·降档"。只加在实际买点文案
    （含"买点/企稳可买入"）上，破位/等待类不加。
  - 每日报告：买点表新增"市场"列 + 市场状态摘要行；启动加仓候选动作列自适应；
    主升区/量能不足时表格上方加警告行。
  - 盯盘页：启动卡在主升区/量能不足时顶部加红色/琥珀警告条；evaluate_alert
    对"仅观察/量能不足/轻仓"的启动信号不提醒打底仓；"✓ 买点1/✓ 企稳"判断从
    startswith 改为 in（兼容市场前缀）。
- 测试：test_monitor.py +2（自适应动作三态、主升区节制提醒、买点前缀）、
  test_market_state.py +2（launch_action/t0_note），**75 项全绿**。
- 待部署：monitor/tracker/market_state/tests/AGENTS 同步服务器 + 重启
  mainrise-monitor。

## 修改（2026-08-13，T0 止损收紧 -5% → -4%）

- 用户先提议"主升区止盈收紧 6%、杀跌区放宽 10%"，用 T0 全样本（914 笔）按
  大盘状态 × 止盈(6/8/10/12%) × 止损(4~7%) 网格验证：
  - **止盈侧结论与直觉相反**：主升区收紧到 6% 反而最差（PF 1.43 vs 8% 的 1.50、
    10% 的 1.58，提前截断赢家）；杀跌区 8% 最优（10% 反降）；差异均小（±1-2pp）。
  - **止损侧 4% 全区间略优**：主升区 PF 1.71 vs 5% 的 1.50、震荡 2.25 vs 2.04、
    杀跌 2.36 vs 1.98（杀跌区 n=58 样本小）。
- 用户确认：**止损统一收 4%**（理由：这两天都是破位割肉，收紧止损控制单笔亏损）。
  止盈维持回落 8% 不变（数据不支持自适应）。
- 改动范围：monitor.py（STOP=0.96 + 文案）、tracker.py（STOP=0.96 + 报告文案）、
  backtest.py（v3 纪律 0.96，未来回测按 4% 验证）、scripts/backtest_market_volume.py、
  signals/excel_report/web_dashboard/help_content/report.py 文案；
  **三态阈值 -5%（杀跌区定义）不受影响**。
- 测试 75 项全绿；待同步服务器重启生效。

## 修改（2026-08-13，信号卡名称/代码可点）

- 实时盯盘"今日买入卖出信号"卡第一列改为"名称/代码"（与主表一致）：
  名称在上（加粗可点）、代码在下（灰色小字可点），点击直接进 /stock/<code>
  K线/分时页；表头由"代码+名称"两列合并为一列。
- JS 侧同步：`sigRowHtml` 新行结构、`upSig` 列索引 3-7 → 2-6（现价/涨跌/量比/
  买入/卖出），盘中 3 秒轮询动态增删行不串列。
- 测试 +1 断言（`名称/代码` 表头），75 项全绿；服务器已同步重启生效。

## 研究（2026-08-14，秦安股份 8/6 试盘形态）

- 用户提出 603758 秦安股份 8/6 是"经典技术派买入信号、才符合 T0"，已解剖+回测，
  报告 `docs/秦安股份0806试盘形态研究.md`：
  - **8/6 形态**：深度回调（距前高-29%）+ 地量筑底（换手0.5-0.75%）+ 均线首次
    多头 + 放量（量比2.75）盘中冲高+6.7% 但收盘仅+1.9%（试盘）+ 长上下影；
    分时尾盘14:30后放量急拉（占全天量72%），收盘站上均价。8/7-8/12 四连板。
  - **T0 为何漏掉**：T0 用收盘涨幅（1.93%<5%）和收盘创20日新高（未过9.91），
    盘中 high 9.98 已新高但不算；8/7 触发时买点日 8/11 一字板买不进。
  - **量化规则（冲高试盘）**：底部（距60日低点<20%）+ MA5>MA10 + 量比≥2 +
    盘中冲高≥5%且收盘<3% + 换手<5% + **次日不破低点确认**（关键）。
  - **回测**：全市场 2143 笔 10日胜率53.7%/均收+2.43%/PF 2.05（试盘条件有
    增量：PF 1.73→2.05）；次日涨停率仅 3%——不是必抓涨停，靠头部大牛贡献
    （秦安 r5 +47.6%）；卡点名单仅 26 笔（61.5%/PF 2.92）。定位为 T0 预备
    **观察/加分项**，不建议直接进买点；603758 不在卡点名单。
  - 数据：本地日线 + 新浪 5 分钟（东财历史分时接口限流，降级用新浪）。
- 待办：如要接入，做 `mainrise launch` 第三信号源 + 卡点名单纸面跟踪。

## 研究（2026-08-14，均线粘合爆量突破 → T0 时点前移）

- 用户洞察：现 T0 要求"多头排列"（MA5>MA10>MA20），触发时均线已发散、多在高点
  站岗；要抓"均线粘合 + 放量阳线上穿 5/10/20 日均线（刚发散）"的那一根。
  已验证，报告 `docs/均线粘合爆量突破研究.md`：
  - **站岗问题属实**：现 T0（卡点名单，2022 起）10 日口径 43.5% 胜率/PF 1.20，
    **64% 信号 10 日内回撤>5%**、最深回撤均值 -8.9%。
  - **新信号 B3**：粘合≤3%（MA5/10/20 最大偏离）+ 阳线 + 量比≥2 + 涨幅≥1% +
    close 站上三均线 + 距60日低点<30% + 次日不破低点确认 → 次日开盘买。
  - **B3 全面胜出**：n=1244，10日胜率 51.0%（+7.5pp）、均收 +1.43%、PF 1.48、
    回撤>5% 39%（-25pp）、最深回撤 -5.2%；代价是 P≥10% 15.2% vs 19.5%
    （少抓已发散狂奔的大牛）→ 建议 B3 做第一级信号、现 T0 降为追强第二级。
- 秦安 8/6 命中 B3 全部条件（涨幅≥1% 版本；原 T0 用≥5% 必漏）。
- 待办：B3 接入 signals/backtest 前先加参数组做 v3 纪律口径全样本验证。

## 研究（2026-08-14，两级结构：B3 第一级 → 回调粘合 → 二波启动 = 最优买点）

- 用户强调"看盈亏比不看胜率——失败止损兜底、成功翻倍"，并定义两级结构：
  B3 第一级 → 回调一波均线再次粘合 → 再触发启动 = 走牛概率最高的买点。
  用带纪律交易模拟验证（卡点名单 2022 起，次日开盘买，止损-4%/高点回落8%/
  时间止损）：
  - **二波（B3→深回调2-12%→粘合≤2%→缩量→次日放量阳线站上三均线→次日开）**：
    n=401；10日止损口径 胜率66.6%/均收+6.76%/**PF 6.98**/盈亏比3.50/最大+103%/
    最差-4%；20日口径 胜率57.6%/均收+9.08%/PF 6.55/盈亏比4.82/P≥20% 18.5%/
    P≥50% 5.2%。
  - B3 第一级对照：PF 1.81（10日）/2.19（20日）——二波显著更强。
  - **此前"二波 45% 胜率"是裸收益口径误导**：止损把深亏尾部封顶后胜率升至
    57-67%。用户框架成立。
- 接入方案（待用户确认）：signals.py 两级信号（B3=第一级观察，二波=真买点），
  backtest 参数组按 v3 纪律（-4%/回落8%）验证；详见 docs/均线粘合爆量突破研究.md。

## 功能（2026-08-14，两级模型已接入：B3 打底仓 / 二波加仓）

- 用户确认逻辑："1. B3 提示打底仓 2. 最优买点加仓"，已按此修改：
  - `signals.tail_features` 新增 `b3`（粘合≤3%+阳线+量比≥2+涨幅≥1%+站上三均线+
    距60日低点<30%）与 `wave2`（B3 后 3~30 日 深回调2-12% + 均线再次粘合≤2% +
    缩量 → 放量阳线再启动）两档；内部窗口至少 90 行（lo60 60 日 + 二波 30 日，
    否则 tail=60 时全部漏判）。
  - 日报新增"六、两级模型"节 + 写 `output/state/mainrise_twostage.csv`；
    盯盘页读取并提醒"明日开盘打底仓（2/3）/ 明日开盘加仓（1/3）"。
  - `mainrise backtest --two-stage`：无前视纪律模拟（B3 S+1确认→S+2开；
    二波 T+1 开；止损-4%/回落8%/10-20日）。
- **⚠ 回测前视偏差已修正**：初版"二波 66.6% 胜率/PF 6.98"用确认日低点确认又
  同日开盘买（做不到）；无前视后真实为：B3 10日 43.8%/PF 1.81、20日 38.9%/
  PF 1.94；二波 10日 39.3%/PF 1.65、20日 36.1%/PF 1.73、盈亏比 2.6-3.1、
  最差 -4%（止损封顶）、尾部 +99% 级——**低胜率高赔率，两级仓位结构成立**。
- 测试：test_signals.py +2（b3/wave2 合成用例），**82 项全绿**。
- 坑：tracker 函数内重复 `from mainrise.signals import tail_features` 会遮蔽
  模块级导入导致 UnboundLocalError（已删）；空信号 CSV 要固定列头（否则
  后续读取 EmptyDataError）。

## 修复（2026-08-14，信号卡名称/代码被现价覆盖）

- 现象：买入卖出卡片第一列（名称/代码）变成现价，名称和代码消失。
- 根因：`upRow`/`redrawSpark`（主表 3 秒轮询更新）用**未限定作用域**的
  `querySelector('tr[data-code=...]')`，而信号卡（sigtb）在 DOM 中位于主表
  （mtb）之前且行也带 data-code → 主表更新函数先命中卡片行，把卡片第 1 列
  （昨日起为名称/代码）当主表"现价"列覆盖。
- 修复：主表两个更新函数限定 `#mtb tr[data-code=...]`（upRow/redrawSpark），
  卡片本就限定 `#sigtb`；测试新增断言（`#mtb tr[data-code=` 必须存在于页面 JS），
  **76 项全绿**；服务器已同步重启，live.html 重新生成生效。
- 服务器测试：PyQt5 缺失导致的 test_settings ImportError 为已知环境差异，
  其余全绿（70 项）。

## 会话记忆（2026-08-14，大牛模型固化 + 门户重做）

研究链（全部无前视、100/97 只卡点范围、费用 0.2%）：
- **买点提前研究**（`mainrise entry-study`）：入场时点阶梯 T0尾盘 > T1开盘 > T2开盘基线
  ——T0 尾盘买入（14:50-15:00，信号日收盘近似）四项全优（38.8% vs 基线 37.8%）；
  确认是"加仓触发"不是"入场条件"；杀跌区（30%）与市场涨停≥130 过热日（26.9%）是站岗重灾区。
- **趋势大牛研究**（`mainrise bigtrend`）：AI硬件 主题信号大牛率 44.6%（基准 29.7%）；
  观察点=热主题+创60日新高+10日<30%（50.5%）；T1 确认是加仓触发；主仓跌破 MA60 才走
  （**修复 2026-08-14：原"+85%"是死规则假数据——tail_features 从未算 ma60，
  "跌破MA60"统计实为死拿 150 日窗口末；补算 ma60 后真实退出 +33.6%/持仓 57 日/
  回吐 44.9%，方向结论不变但数字修正**），紧退出（8%/10% 回落、跌破 MA10/MA20）
  会被深度洗盘甩下车。
- **波段高抛低吸研究**（`mainrise wave`）：zigzag 顶/底确认日成交口径下，死拿 > 波段
  （大牛组 +84.5% vs +69.1%）——确认滞后+费用吃掉波段收益；波段的真实价值在非大牛少亏
  与暴跌年躲回撤（2026 波段 +25.4% vs 死拿 +6.8%）。
- **买入信号计数研究**（`mainrise bullcnt`）：**T0 计数是"大牛属性"判别信号**
  （90日内 1→6 个 T0，大牛率 22%→43% 单调上升）；**B3 计数不判别大牛**
  （1→5 个反而 27%→19%，宏和主升期仅 1 个 B3）——B3 是入场点信号不是属性信号。
- **大牛候选回测**（`mainrise candidate-bt`）：90日≥3T0+热主题 抓到 81 只全样本大牛
  中的 32 只（密度 24% vs 基线 13%）；MA60 退出。
- **组合级回测**（`mainrise portfolio-bt`）：真实资金账户（1/3仓+3只上限+净值曲线）。
  **MA20 退出 + 杀跌区停开 大幅优于 MA60**（+614% vs +310%，回撤 -34% vs -51%）；
  **企稳买回是负优化**（站回即买回仅 +163%，301 次买回胜率 24%）；弱市降档四项全改善。
- **大牛模型固化**（`mainrise bigbull`，2026-08-14 用户确认）：
  **硬规则·评分≥2 + MA20 退出 + 杀跌区停开**，97 只范围（排除 301/688，300 可交易）。
  回测 +777% / 年化 +50% / 最大回撤 -34% / PF 2.96 / 胜率 44% / 108 笔全平仓
  （2025 +146%、2026 +102%）。输出 `大牛模型回测_*.md` / `大牛模型交割单_*.csv` /
  `大牛模型净值_*.csv` / 门户 `output/web/index.html`。
- **门户重做**：index.html 只含模型有效信息（市场状态/近期候选信号/净值曲线/
  最近交割记录/模型规则卡），旧 dashboard/live/reports 页面不再链接但保留文件。
- 新增模块：entry_study/bigtrend/wave/bullcnt/candidate_bt/portfolio_bt/bigbull，
  测试 **103 项全绿**；研究文档归档 docs/（买点提前与胜率研究/趋势大牛研究/
  波段高抛低吸研究/买入信号计数大牛判定/大牛候选规则回测/大牛候选组合回测/大牛模型）。
- 关键负结果（勿回退）：企稳买回负优化；评分是质量过滤器不是收益增强器（≥3 提高
  胜率/PF 但总收益 +670% < +777%）；B3 计数不判别大牛；2021-2024 弱年该模型回撤深
  （2022 -15%），需三态轮动降档。

## 会话记忆（2026-08-14，服务器部署 + 盯盘接入 + 微信推送）

- **大牛模型部署腾讯云**：rsync 代码链（**必须用 -R 保留目录结构**，曾因未加 -R
  把 mainrise/bigbull.py 拍平到服务器顶层）+ 服务器跑 bigbull（108 笔交割单/净值/
  门户/候选JSON）；daily_update.sh 改为 `track --no-dashboard` + bigbull（移除
  web/review，门户只由 bigbull 生成）；服务器旧门户页（dashboard/reports/about/
  review/reports_view）已清理，output/web/ 只剩 index.html + live.html/json。
- **盯盘页接入大牛模型**：live.html 新增"大牛模型候选卡"（实时现价/涨跌/量比/
  距MA20/评分/信号日/状态列），候选加入 monitor 3 秒轮询；盘中 T0 近似
  （涨幅≥5% 且量比≥1.5 或涨停、站上 MA20）→ 提醒 + 卡片琥珀高亮"今日或成T0·
  14:50尾盘确认"；门户候选表今日新信号高亮【今日新信号】。
- **OOM 事故（重要教训）**：monitor 每 3 秒触发 load_all_panels()（700 万行）
  刷新候选 → 服务器 2C4G 内存耗尽 OOM 死循环（CPU/内存 90%+、SSH 饿死、
  用户重启服务器才恢复）。修复：候选改由 bigbull 每日落盘
  `output/state/bigbull_cands.json`，monitor **只读文件（60s 缓存）**，盘中零
  全量计算。教训已写入"已知决策与坑"。
- **Server酱微信推送**：新增 `mainrise/push.py`（`mainrise push` / `--test`）：
  每交易日 14:50 cron 推送 今日买入（盘中 T0 近似）/ 卖出（持仓止损/回落/破位）/
  市场状态；数据源=monitor 的 live.json（非今日自动跳过）；key 存
  `.serverchan_key`（600，已加 .gitignore，**不写入 AGENTS/仓库**）；本地与服务器
  测试推送均成功（微信已收到）。
- 测试：新增 test_push.py 6 项，**109 项全绿**；本地与服务器均已同步。

## 功能（2026-08-14，17:30 收盘确认推送）

- 用户在 14:50 尾盘决策推送基础上要求把 **17:30 收盘确认**也加入每日流水线。
- `push.py` 新增 `mainrise push --close`（`run_close`）：读 bigbull 每日落盘
  `output/state/bigbull_cands.json`（updated / mkt.mkt_ret20）+ `output/reports/
  大牛模型交割单_*.csv`（中文表头，utf-8-sig），**只做文件读取不做全量重算**
  （沿用 OOM 教训）；`build_close_message` 组装：今日触发信号数 → 确认买入
  （信号日收盘价，补评分/90日T0/距MA20，按候选 JSON 代码匹配）→ 确认卖出
  （卖出日期=今日 且 状态=已平仓，收盘跌破 MA20）→ 当前持仓（状态=未平仓，
  卖出价列=最新收盘、收益率为浮动盈亏）→ 大盘状态（≤-5% 杀跌区停开）。
- **日期保护**：bigbull 候选 updated 或交割单文件名日期 ≠ 今日 → 跳过（非交易日/
  当日行情未出/数据缺口不误报）；`--dry-run` 只打印不发送（**不消耗 Server酱
  免费配额**，用户当日剩余 1 次，明确要求不测试发送）。
- 交割单 CSV 中未平仓行（open=1）的 卖出日期=最新数据日、卖出价=最新收盘、
  收益率=浮动盈亏（portfolio_bt.simulate 语义），收盘推送据此展示持仓，无需
  额外取数。
- `bigbull.py`：`write_cands_json` 增加 `mkt.mkt_ret20` 字段（NaN 不写），
  `run()` 落盘时传入 `mkt_ret20.get(last_date)`，供 17:30 推送直接读取。
- `daily_update.sh`：bigbull 后追加 `"$PY" -m mainrise.cli push --close`（17:30
  cron 同一流水线内完成；cmd_push 不因推送失败中断流水线）。
- 测试：新增 test_push.py 收盘确认 6 项（消息构建/弱市/CSV 解析/日期过期跳过/
  缺 CSV 跳过/dry-run 全链路），**115 项全绿**；本地 dry-run 验证跳过与渲染
  均正常，未做任何真实发送。

## 修复（2026-08-14，交割单 CSV 列名 code→代码）

- 排查"服务器模型买点与回测买点一致性"时发现：`bigbull.run()` 写交割单 CSV 时
  rename 漏了 `code`，首列一直是英文 `code`（其余中文）→ **17:30 收盘确认推送
  （push --close）读交割单时代码列取空**（load_trades_csv 兜底补空列掩盖了问题）。
- 修复：提取 `bigbull.trades_to_csv_frame(tr, names, theme_of)` 统一中文表头
  （代码/名称/主题 + 买入/卖出/收益率/状态…）；`run()` 改调该函数。
- 测试：test_bigbull 新增 `test_trades_csv_chinese_columns`（断言 代码 在列、
  code 不在列），**116 项全绿**；本地 + 服务器重跑 bigbull 验证列名与
  push --close 渲染均正常。
- 一致性实证（服务器 08-14 最新数据）：模型当前空仓（最近一笔 07-07 柯力传感
  止损）；07-21 000938 硬规则信号（评分2/计数4）未买入——当日大盘等权20日
  **-7.16% 属杀跌区停开**，规则正确执行；07-17~22 连续杀跌区（-5.7%~-8.5%），
  此后至 08-13 无新硬规则信号 → 空仓符合纪律。
- **口径说明（设计内差异，非 bug）**：14:50 尾盘决策=monitor 盘中 T0 近似
  （站上MA20 + 涨≥5%且量比≥1.5 或 涨≥9.5%，不查创20日新高/90日T0重算/评分/
  仓位上限/杀跌区），名单范围=bigbull 候选池（已过硬规则+评分），是"候选池内
  今日可能成T0"的预警；17:30 收盘确认=交割单（回测 simulate 当日入场），与
  回测买点逐字同源（bigbull 每日重跑全历史模拟）。14:50 卖出=纸面持仓（框架
  持仓），17:30 卖出=大牛模型回测持仓，**两个持仓池口径不同**。

## 修改（2026-08-14，14:50 推送卖出切大牛模型持仓口径）

- 用户要求 14:50 推送的卖出与买入一致，切到大牛模型持仓口径（不再用框架纸面持仓）。
- `bigbull.py`：`write_cands_json` 新增 `holdings`（交割单未平仓 + 最新 MA20/收盘/
  买入日/成本/评分，从 tr[open==1] + info 构建），落盘 `bigbull_cands.json`。
- `monitor.py`：`_BB_FILE_CACHE` 重构为缓存整份数据，新增 `_bigbull_holdings()`；
  `load_monitor_rows` 把持仓并入 rows（group=大牛模型、status=大牛模型持仓、
  bb_hold=True）；`build_state` 持仓分支：buy=持有中，盘中 px<MA20 →
  sell="⚠ 跌破MA20（现价 x < MA20 y）→ 收盘确认卖出"，否则"守MA20"；
  提醒 "📉 大牛模型持仓：…收盘确认卖出"（cooldown bbh_）；live.html 候选卡
  显示持仓（状态=持仓/⚠持仓破MA20，标题含"持仓 N 只"）。
- `push.py` 14:50 消息卖出：只取 group=大牛模型 且 bb_hold 且 sell 含"跌破MA20"；
  标题/空态文案更新；**框架纸面持仓不再出现在推送**（盯盘页仍监控）。
- 测试：test_push 卖出口径断言（bb_hold 计入、框架持仓不计入、守MA20 不计入），
  test_monitor 新增 bb_hold 破位/守位两例（build_state + 渲染 + 提醒），
  test_bigbull 新增 holdings 落盘断言，**120 项全绿**。
- 部署：rsync + 服务器重跑 bigbull（holdings 键就位，当前 0 持仓=空仓）+
  systemctl restart mainrise-monitor + 手动一轮 run_one_cycle 验证
  live.json（29 只、6 大牛模型行、0 持仓）与 live.html 渲染分支正常。
- 口径备注：大牛模型持仓由 17:30 bigbull 交割单定义，盘中按昨日持仓盯 MA20；
  14:50 为盘中预警，17:30 收盘确认卖出（卖出确认=收盘 < MA20 的交割单行）。

## 修改（2026-08-14，门户 5 项改造）

- 用户 5 点要求（bigbull.write_portal）：
  1. **买卖点提醒卡**：新增常驻卡片（今日买点=当日硬规则信号+评分/90日T0/提示，
     杀跌区标注停开；今日卖出=收盘破MA20 的交割单行；当前持仓=未平仓+现价/距MA20/
     守MA20 或 ⚠破MA20→收盘确认卖出），空态有占位文案（"今日无硬规则信号/当前空仓"）。
  2. **候选名称+代码合并**：上名称下代码，代码 `<a href="/stock/<code>">` 直达
     看盘页（monitor 内嵌 8765 服务 + Nginx 反代，与盯盘页同链路）；新增 `_nc()`
     辅助函数。
  3. **候选去重**：`model_candidates` 按代码去重（保留最近一次信号，列表已按日期
     倒序取首个）；cands JSON 同步去重（monitor 本就按代码合并）。
  4. **交割记录倒序**：`sort_values("exit_date", ascending=False).head(20)`。
  5. **收益曲线重置**：新增 `bigbull.PORTAL_REBASE = "2025-01-01"`，净值曲线以
     该日起点重置为 1.0，KPI（总收益/年化/回撤/PF/胜率/笔数）与交割记录同步取
     窗口口径；完整历史回测保留在页脚（total_full/cagr_full/...）与大牛模型回测 md。
- 测试：test_bigbull 重构（_min_inputs/_portal 辅助）+ 新增 买卖点持仓卡断言、
  交割倒序、model_candidates 去重，**123 项全绿**；本地 + 服务器重跑 bigbull，
  门户 5 项逐条验证通过（候选直达代码 6 只无重复）。
- 部署：rsync bigbull.py + test_bigbull.py → 服务器重跑 bigbull（门户/cands 已更新）。

## 修改（2026-08-14，门户重置起点改为 2026-08-01）

- 用户要求："净值曲线从 2026 年 8 月开始重置，不需要看之前的收益，主要跟踪后续
  的收益"。`bigbull.PORTAL_REBASE` 由 "2025-01-01" 改为 **"2026-08-01"**。
- 效果：净值曲线从 2026-08 首个交易日以 1.00 起画（当前 9 个交易日），KPI 与
  交割记录同步取窗口口径；窗口内无平仓时胜率显示 "—"（新增 win_txt 防 NaN）。
- 现状说明：模型 8 月以来空仓无交易 → 曲线平在 1.00、交割记录为空，属真实状态，
  后续模型开仓后曲线/记录从这里开始累积。
- 测试：test_bigbull 数据跨度改至 2026-12（窗口为真实子集），新增
  test_portal_rebase_from_2026_08（起点日期+净值 1.00+标题口径断言），
  **124 项全绿**；本地 + 服务器重跑 bigbull 验证（服务器曲线 9 个交易日、
  起点净值 1.00、无 2025 残留）。

## 功能（2026-08-14，服务器运维增强：企业微信 / 告警 / 健康巡检 / 周报 / 夜间研究）

用户选 4 项落地：
1. **企业微信推送渠道（免费不限量）**：`push.py` 新增 `get_wecom_webhook()/
   send_wecom()/run_wecom()/send_alert()`；webhook 配置 WECOM_WEBHOOK 环境变量 →
   settings.json wecom_webhook → `~/.wecom_webhook`（600，gitignore）；`push --wecom`
   用企业微信发 14:50 消息（绕开 Server酱 5 次/天）；`alert "标题" "正文"` 通用告警
   （企业微信优先、Server酱兜底，`--dry-run` 防误发——教训：验证 alert 时曾真实
   发送一条测试消息占用 Server酱 1 次额度，现已加 --dry-run 规避）。
2. **流水线失败告警**：`daily_update.sh` 加 EXIT trap，任一步失败 → 尾日志 25 行
   推微信（alert 命令）。
3. **健康巡检**：新增 `deploy/health_check.sh`（工作日 09:05 cron）：monitor 服务
   存活 / 数据新鲜度（今天若为交易日，bigbull_cands.updated 应≥昨天，按
   trade_dates 判交易日）/ 磁盘≥85% / 内存<300MB → 异常推微信；手动验证一次
   "健康巡检正常"。
4. **每周绩效小结**：新增 `mainrise/weekly.py` + CLI `weekly`（周五 17:35 cron）：
   读最新交割单/净值/候选，汇总本周买入/卖出/持仓/信号/净值变化（"本周"=净值曲线
   最后 5 个交易日，非自然周避免节假日错位）；发送企业微信优先、Server酱兜底。
5. **夜间研究任务**：新增 `deploy/night_research.sh`（周日 22:30 cron）在服务器跑
   entry-study/bigtrend/wave/bullcnt/candidate-bt/reversal/launch/strategy 8 项
   研究 → output/reports + 完成告警；**发现并补齐服务器缺失的 bullcnt.py**（本地
   有、服务器漏同步，已 rsync + 测试）。
- 服务器 cron 现状：17:30 每日流水线、14:50 尾盘推送、09:05 健康巡检（1-5）、
  周五 17:35 周报、周日 22:30 夜间研究。
- 测试：test_push 新增 TestWecom 5 项、test_weekly 2 项，**131 项全绿**；
  本地 + 服务器 CLI 验证（weekly --dry-run 渲染真实数据、alert --dry-run）。
- 待办：用户需创建企业微信群机器人并提供 webhook URL，写入本地与服务器的
  `~/.wecom_webhook` 后，告警/周报/复盘推送即不再消耗 Server酱额度。

## 研究（2026-08-14，热主题动态化：四方案全负优化，保持固定）

用户连续指正后完整探索"跟随市场动态的热主题"，四条路线全部回测验证：
1. **日线动量 Top3**（模型池主题等权20日涨幅）：+281%，MDD-48% —— 追高+频繁切换。
2. **日线均线趋势**（指数>MA20&MA20>MA60，同花顺行业指数）：2022+ +293~477%，
   40% 交易日无热主题空仓踏空。
3. **月线级排名 Top10**（用户指定方向：同花顺全A行业指数 N 日涨幅排名取前10，
   lookback 20/60/120 变体）：2022+ 最优 +563%（@120日）/MDD-33%，仍差于静态
   +679%/MDD-30%。
4. **放开热主题硬门槛**（全97只，只按90日T0≥3+评分）：+102%/MDD-80% —— 最差，
   2022-2024 乱买（-53%/-18%/-8%）。
- **结论**：固定 AI硬件/半导体/存储 恰锁 2022-2026 科技超级周期（AI算力/半导体
  国产化/存储涨价），是模型最大 alpha 来源；热主题动态化（无论粒度）都是负优化。
  **默认 HOT_MODE="static" 不动固化模型 +777%**。
- 新模块 `mainrise/themeindex.py`（CLI `themeindex`）：同花顺 hithink REST
  （fuyao.aicubes.cn，Key 复用 settings.json api_key / ~/.hithink_key）拉 8 主题
  映射的粗行业指数（半导体/通信设备/计算机设备/光学光电子/专用设备/机器人/
  航天装备/军工装备/汽车整车/汽车零部件/化学制药/生物制品/工业金属/小金属，
  320 行业清单 tag=industry）日K（实测 .TI 行业指数历史仅 2022-01 起），
  rank_top=10 排名模式 + 二元趋势模式；K线缓存 output/state/ths_kline/（TTL 1天）。
  `bigbull.HOT_MODE="dynamic"` 一键切换（2022 前回退静态 HOT_PRE_FALLBACK）。
- 基础设施：portfolio_bt.simulate 新增 hot_by_date 按日期热主题（向后兼容）；
  push --close 消息加"今日热主题"行；门户市场状态卡显示当前热主题（模式标注）。
- 测试：test_themeindex 3 项（Key/趋势/代码映射）、test_bigbull 候选/门户热主题
  断言、test_portfolio_bt hot_by_date，**137 项全绿**；删除被否决的 hotthemes.py
  （模型池版）。服务器已同步（含 .hithink_key 600）+ bigbull 验证
  （热主题 ['AI硬件','半导体','存储'] 落盘 cands）。

## 会话记忆（2026-08-14，审查报告 8 高危修复）

`审查报告.md`（4 并行只读审查 agent 产出，158 项全绿基线）发现 8 高危 +
~20 中等 + ~30 低危。用户确认修全部 8 个高危（本轮完成）：

- **H1 bigtrend ma60 死规则（研究结论可信度）**：tail_features 只输出
  ma5/10/20 从不含 ma60 → exit_rules 里 "跌破MA60" 恒不触发，统计实为死拿
  150 日窗口末，"大牛信号平均 +85%" 是假数据。修复：exit_rules 本地补算
  `pd.Series(closes).rolling(60).mean()`（与 candidate_bt/portfolio_bt 一致），
  重跑后 ma60 真实退出 **+33.6%/持仓57日/回吐44.9%**（方向结论不变：宽松退出
  优于紧退出，但数字已修正，AGENTS 同步）。
- **H2 monitor 冷却键全局 → 按票**：止损/回落8%/破MA10/破MA20 冷却键原为
  "stop"/"pullback"/"break20"/"break10"（无 code），急跌日多持仓同日破位时
  第一只触发后 10 分钟内其余持仓卖出提醒全漏。修复：键加 `_{code}` 后缀
  （B3/二波/启动原本就按票）；evaluate_alert 内取 `code = str(row.get("code") or "")`。
- **H3 monitor 主循环无兜底 → 单轮异常继续**：run_one_cycle 原无 try/except，
  东财兜底 `_chg_span("-")` TypeError 会杀死盯盘服务（systemd 拉起但丢冷却状态）。
  修复：while 循环内 run_one_cycle 整体 try/except，打印异常 + alerts 追加
  "盯盘异常已自动继续"，继续下一轮。
- **H4 GUI 按钮 need_key=False**：evaluate/snapshot 已迁腾讯/东财免费接口、
  文案教"可留空"，但按钮仍 need_key=True 空 key 直接弹窗 return。修复：两处改 False。
- **H5 entry_study 买点2 恒空章节 → 下线**：过滤条件 `label != "回踩低吸"` 但
  row_status 永不返回该标签（只返回 二波加仓/B3打底仓/B3待二波/观察）→ 报告
  恒"共 0 个"无报错。修复：章节明确下线（旧 T0/T1/T2 框架 2026-08-14 移除，
  现入场仅 B3/二波），结论第 5 条同步改为下线说明，移除 row_status 导入与
  s_ma10/s_close 计算。
- **H6 测试污染真实状态文件**：test_price_events setUp 设 `paths._ROOT/_MODE`
  但 paths.py 无此属性 → 隔离死代码，测试 mock 数据写进真实 output/。
  修复：paths.py 实现 **`_OVERRIDE` 注入根**（home/report_dir/state_dir/web_dir
  短路），测试改设 `paths._OVERRIDE = tmpdir`，tearDown 清 None；已验证测试后
  真实 price_events.json mtime 不变。
- **H7 部署路径三处矛盾 → 统一**：setup_server.sh 默认 /root/chaoduan（nginx
  www-data 无法穿越 0700 /root → 全站 403）vs monitor.service/README 硬编码
  /home/ubuntu/chaoduan。修复：setup_server.sh 默认改 /home/ubuntu/chaoduan
  （与已部署服务器一致，nginx_web.conf 的 sed 占位不变）。
- **H8 health_check 周一必误告警**：exp=昨天（自然日）但数据 updated=最后
  交易日 → 周一 09:05 固定报 stale（告警疲劳掩盖真故障）。修复：预期改为
  **今天之前最近交易日**（trade_dates 里取 prev[-1]，周一预期上周五）。

测试：新增 test_monitor.test_cooldown_is_per_stock（H2 按票）、
test_price_events 路径隔离验证；全量 **158+ 项全绿**。服务器已同步
（bigtrend/monitor/entry_study/paths/gui_pyqt/bigbull/market_state/tests/
deploy 脚本）+ monitor 服务重启验证（19 项测试过、服务 active）。
中等问题（M1-M24）与低危项未动，留待后续批次。

## 会话记忆（2026-08-14，审查报告中等问题修复）

`审查报告.md` 中等问题（M1-M24）本轮全部处理：

**模型与研究**
- **M1 HOT_MODE=dynamic 自毁修复**：主题池仅 8 个而 HOT_RANK_TOP=10 →
  全选=放开门槛（已被证伪 +102%/-80%）；themeindex rank 上限
  `min(rank_top, len(columns)-1)` 至少留 1 个非热。默认 static 不受影响。
- **M2 打包缺名单 + 兜底换池**：build_gui_macos.sh / build_gui_windows.bat
  补 `industry_info.csv` add-data（缺失则 GUI 卡点范围静默缩水 110→36）；
  report.py 内置兜底 INFO 删除 12 只已从 CSV 删除的旧观察池股（现 24 只
  全部与 CSV 一致，孤儿 002197/003032/300031/300191/300497/300540/600236/
  600499/600801/603317/605305/605499 已移除）。
- **M3 账本与"明日开盘"规则对齐（重要）**：B3/二波建仓原来信号日收盘价
  当天买入（隔夜跳空不体现）；改为**信号日登记 pending → 次日数据到位用
  开盘价建仓**（已有底仓的二波加仓同样次日开盘 底仓2/3+加仓1/3 加权，
  reason="B3底仓+二波待加仓"→"B3底仓+二波加仓"）。新增
  test_tracker.test_b3_entry_waits_for_next_day_open 验证。
- **M4 portfolio_bt 空交易 KeyError**：metrics 开头 `if len(tr)==0` 短路
  （零交易返回占位行，不再空表列访问）。
- **M5 load_theme 独立模式崩溃**：bigtrend.load_theme 原
  `paths.home()/mainrise/resources/...` 相对路径打包版不存在；改为
  `Path(__file__).parent/resources/industry_info.csv`（与 report.py 一致）。
- **M6 二波回测口径**：backtest.py 二波窗口原 `break` 只记第一个满足日，
  与 signals.py wave2 逐日判定不一致；去掉 break 逐日记录（二波 612 笔）。
  `--two-stage` flag 已注明"默认即两级模型"（兼容保留，非 bug）。
- **M7 evaluate 取数列验证通过**：真实调用确认 XSMLL(毛利率 89.76)/
  WEIGHTAVG_ROE(10.57) 均正常返回（非幽灵列，评级无系统性偏差）。

**推送/安全**
- **M8 push 用北京时间**：push.py 加 `beijing_now()`（UTC+8 固定偏移），
  两处 `datetime.now()` 替换（原用服务器本地时间，非 CN 时区会错日期）。
- **M9 push --close --wecom 静默忽略 close**：cli.py 组合时打印警告
  （企业微信渠道只发 14:50 尾盘，close 走 Server酱）。
- **M10 settings.json 未进 .gitignore**：补 settings.json / .monitor_env
  （push 项目模式读项目根 settings.json，防 key/webhook 泄露）。
- **M11 cron 安装入口补齐**：setup_server.sh 从只装 17:30 一条改为 5 条
  （17:30 daily / 14:50 push / 09:05 health_check / 周五 17:35 weekly /
  周日 22:30 night_research）。

**文案/界面一致性**
- **M13 时间止损文案统一**：tracker 平仓 reason/报告头、report.py 写死
  "5日"改 `TIME_STOP`（20 日）动态；excel_report 已是"10-20"保留。
- **M14 仪表盘防追高阈值同步**：dashboard DEFAULT_MAX_10D_GAIN 80→150
  （与 tracker.MAX_10D_GAIN 一致）；help_content "≥80%"→"≥150%"。
- **M15 README 旧模型描述**：买点1/买点2/止损-5%/5日 → 大牛模型 + B3/二波
  两级模型（-4%/20日）双段更新。
- **M16 help_content 自相矛盾**：实时行情/财务评估"需要 API Key"红字 →
  "无需 Key（公开接口）"；"仅本次会话有效"→"写入 settings.json 自动记忆"。

**运维脚本**
- **M17 chown 失败也执行**：daily_update.sh 的 chown 移入 EXIT trap（任一步
  失败也把 output 归 ubuntu，避免 monitor 次日 PermissionError 级联）。
- **M18 daily_update 交易日判断**：流水线前查 trade_dates，非交易日直接
  exit 0（不再空转/误告警）。
- **M19 systemd EnvironmentFile 加 `-` 前缀**：.monitor_env 缺失不再阻断启动。
- **M20 nginx /refresh 加 proxy_read_timeout 300s**（page=live 分钟级扫描
  防 504）。
- **M21 night_research 超时+成功不告警**：每命令 timeout 2700s（总 45 分钟
  防无限阻塞周日 cron）；只在有失败时告警（成功不占 Server酱 5 次/天）。
- **M22 monitor 交易日历缺失/过期告警**：trade_dates 空 → 打印"将整天不轮询"
  警告；最新交易日距今 >5 天 → "数据可能长期未更新"警告。
- **M23 market_state 接口失败退避**：_index_ret20 失败也推进缓存时间戳
  （不再每 3 秒打腾讯，5 分钟后再试；保留上次成功值）。
- **M24 price_events 风控/异常判定细化**：网络异常/非 jsonp 响应 → 返回
  None 回退腾讯（原 break 返回 [] 静默记 0）；风控（无 cmsArticleWebOld）
  同样回退；_search 外层 try 包 _search_em。

测试：161 项全绿（新增 test_tracker 次日开盘建仓、test_price_events
网络异常回退）。服务器已同步全部文件 + 重启 monitor（23 项测试过）。

## 会话记忆（2026-08-15，全面审计 + 修复）

**三路 agent 审计 + 服务器实测，发现并修复 8 个问题**：

- **H2（正确性，已修）**：全市场口径混入 301 不可交易股（`in_universe` 含 301 前缀，
  465 只）→ 固化指标虚增。bigbull/mktrelax 全市场分支显式排除 301/688。
  **修正后指标：+1108%→+998% / PF 3.67→3.45 / 胜率 48%→46% / 107 笔**。
- **H1（正确性，已修）**：ignite5 盘中 prev_close/hi20 基准滞后 1 日（日线只到昨日时
  `iloc[-2]` 取到前天，实测偏差至 -9%）。修复=按 5分钟最新交易日（day_prefix）与
  日线最新日期是否相等决定用 iloc[-1]/-20: 或 iloc[-2]/-21:-1。
- **H3（安全，已修）**：门户 80 端口明文 HTTP → 自签证书 + 443 + 80→301 跳转
  （`/etc/nginx/ssl/mainrise.crt`，IP SAN，10年）。
- **M2（安全，已修）**：stockpage 详情页名称/原因/错误未转义（存储型 XSS）→ 服务端
  `html.escape` + 前端 `esc()` 覆盖 drawK/drawT/closesInfo/d.error。
- **M3（安全，已修）**：/refresh GET 也有副作用且无 CSRF → 仅 POST + Origin/Referer
  同源校验，GET 405。
- **M1（稳定性，已修）**：ignite5 盘中 cron 每 10 分钟 `load_all_panels()` 全量 700
  万行（OOM 隐患）→ 改为只读候选代码近 40 日按日文件（0.2s/120 行 vs 7.3s/710万行）。
- **M5（稳定性，已修）**：monitor 异常时 append 字符串进 alerts → 渲染 TypeError 冻结
  → 改 dict + isinstance 防御。
- **M7（已修）**：weekly 挪到周五 18:00（避开流水线）。

**审计确认安全**：回测无未来函数（hi20/v5/lo60 均 shift(1)）、below_days 收复清零正确、
top1/多买 NAV 等价、路径遍历堵死（/stock/ 数字过滤）、git 历史无敏感信息、8765 仅本机绑定。

**遗留项处理（2026-08-15 全部完成）**：
- M8 walk-forward 验证（`mainrise walkforward`）：wd=8/top1 在训练段（2021-2023，
  参数选定前）已优于基线（PF 1.59 vs 1.55、MDD -36% vs -39%）；全期 wd=8 断崖领先
  （PF 3.45 vs wd=0 的 3.00）→ **参数稳健非过拟合，+998% 可信**。但 2025-2026 贡献
  约 900pp，高度依赖科技行情延续，回撤可能回到训练段水平（-36~39%）。
- M9 涨跌停约束（`simulate(limit_board=True)`，2026-08-15 用户确认口径）：
  **只有一字板（high==low==涨停/跌停价，实测涨停中仅 8%）才不可成交**；买入口径=
  信号日未涨停尾盘收盘买、一字涨停 T+1 开盘买（pending 队列，次日 open 成交）；
  一字跌停卖不出顺延；停牌不可成交。**回测指标不变（+998%/107 笔）→ 约束是安全兜底**。
- M4 告警降级：`send_alert`/`ignite5.push_signals` 改**飞书→企微→Server酱逐级降级**
  （曾飞书失败即停不发）。
- 门户持仓不过滤 entry_date：2026-08-01 前入场未平仓持仓也显示（`holds = tr[tr.open==1]`）。
- logrotate：`/etc/logrotate.d/chaoduan`（output/logs/*.log daily + 90 天 + 压缩 +
  copytruncate）。
- htpasswd：644→**640 root:www-data**（apr1 哈希保留，因不知原口令无法无损转 bcrypt）。
- m5optimize cron 模板核实为 5 字段（审计 agent 误报，无需改）。

## 会话记忆（2026-08-15，追高弱市禁入规则固化）

**研究链（2026-08-14~15，追高组 17 笔深度回溯）**：
1. 追高入场（chg20≥60% 或 chg10≥30）深亏根源 = **个股透支（chg20≥79%/距60日低点+105%以上）叠加大盘弱市（20日≤0）**——4 笔深亏全部命中至少一条，2 笔两条都命中；而强市透支（600172 大盘+26%、000636 大盘+35%）反而出大牛 → **只看透支会误杀，透支+弱市才该禁**。
2. 次日开盘买入口径（信号日收盘确认→T+1 开盘买）下，"首阴绿就卖"是**负优化**（17 笔均收+3.6%→-0.2%）：次日开盘已吸收跳空，首阴绿多为洗盘（买入当日阴线占 47%，其中 6 笔持有盈利）；"T+1 跌5%走"也只触发 2 笔净效果负。
3. 最佳退出 = **持有至 MA20 不动**（+3.6% 均收）。

**固化规则（2026-08-15 用户确认，`mainrise bigbull` 生效）**：
- **追高+弱市禁入**：信号日 20 日涨幅 ≥60%（透支）**且** 大盘等权 20 日 ≤0%（弱市）→ 禁入。
- 实现：portfolio_bt.simulate 加 `ban_overbought_weak` 参数（bigbull BAN_OVERBOUGHT_WEAK=True，规则卡/报告/门户同步）；build_info 的 sig_feats 补 **chg20** 字段（20 日涨幅，此前只有 chg10）。
- 回测验证：早期测试口径 117→116 笔、+557%→+654%、PF 2.54→2.69、MDD 持平（剔除 2025-01-08 弱市追高 002851 -10.5%，资金滚入后续大牛；2021-2024 零影响）；**最新固化口径 108 笔中 3 笔透支全为强市（正确保留），规则当前零影响零副作用**——只在未来"弱市+透支"信号出现时拦截。
- 大盘 10 日 vs 20 日对比：效果相同（都只净剔 002851 一笔），但 20 日版剔除信号日更少（9 vs 14）误剔风险更低，**选 20 日**。

**顺带修复**：bigbull 门户加导航栏（每日报告/实时盯盘/当日复盘/KPI仪表盘/使用说明）——bigbull 重做门户后旧 reports.html 不再生成、每日报告无入口；现 bigbull 生成门户时同时渲染 reports.html + reports_view/（复用 web_dashboard 函数）。测试更新：test_bigbull 门户断言改为验证导航存在 + 追高弱市禁入规则卡；test_portfolio_bt 新增 test_ban_overbought_weak_gates_entry；test_price_events 日期断言改动态。

测试：**162 项全绿**。服务器已同步（bigbull/portfolio_bt/tests）+ 重跑 bigbull（109 笔 +776%，新规则生效）。

## 会话记忆（2026-08-15，追高弱市禁入同步盯盘/推送）

追高弱市禁入规则（chg20≥60% 且 大盘20日≤0 禁入）从回测扩展到**实盘三端**：
- `bigbull.model_candidates` 新增 `ban_overbought_weak`/`mkt_ret20` 参数，
  与 simulate 同口径过滤候选（信号日 chg20≥60% 且 大盘≤0 → 不列候选）；
  write_portal（门户候选卡）与 run 候选落盘（bigbull_cands.json）都传
  BAN_OVERBOUGHT_WEAK 启用。
- 三端数据流统一：门户/盯盘页/14:50-17:30 推送**全部只读 bigbull_cands.json**
  （monitor/push 不自己算候选）→ 改 bigbull 一处即三端同步，无需改 monitor/push。
- 测试：test_bigbull 新增 test_model_candidates_ban_overbought_weak
  （弱市透支候选过滤/强市保留/关闭保留）；**163 项全绿**。
- 服务器已同步（bigbull.py + test）+ 重跑 bigbull：候选 JSON 正常（3 候选 1 持仓）。

## 会话记忆（2026-08-15，5分钟点火信号系统上线）

**用户方向（2026-08-15）**：候选池标的不能等收盘 T0（涨停价买不进、太迟钝），
需 5 分钟级"资金点火进攻"信号；进入候选池后即 5 分钟追踪，买入信号重新评估。
同时要求**从现在起保留 5 分钟数据到服务器，每月做一次买卖点优化**。

**研究结论（先验证后落地）**：
- 观察池 vs 非观察池在 T0 前 3 日走势无差异（T-1 涨幅 +2.2% vs +2.3%）→
  敏感信号不来自"提前到 T0 前"，而在 **T0 当天盘中**。
- 11 个信号日 5 分钟研究：点火买（+3.1%）vs 收盘买（-0.1%），10/11 更优，
  买价低 0.6~6%（中位 -3.3%）。
- 日线近似"开盘买"（+2145%）有**前视偏差**（开盘时不知当日会触发 T0），
  不可落地；只有盘中实时确认才无前视。

**数据源**：东财 klt=5 只保留近 30 天（服务器 IP 被东财风控不可用）；
**腾讯 m5（ifzq.gtimg.cn）可用**（320 根≈6.7 交易日滚动），选为归档源。

**新模块**：
- `mainrise/m5data.py`：5 分钟归档（腾讯 m5 → data/m5daily/<code>.csv，
  增量合并去重，量×100 手→股）；CLI `m5data`。
- `mainrise/ignite5.py`：盘中点火检测（**2026-08-15 用户明确：点火跟踪只针对
  大牛候选池 bigbull_cands.json 的 cands（已满足买入条件），实测 3 只
  （000938/003031/603662）；不做 90日T0≥2 扩展、不做放量上攻宽松纳入**；
  非交易日保护：周末/节假日不检测并落盘空 signals（防旧信号误报）——
  **点火 = 单根5分钟量 > 2×近5日单根均量 且 当日涨幅≥3% 且（破前20高 或 涨幅≥5%）**；
  收紧后去掉"放量但没涨"的假点火（7→3 信号）；prev_close 用日线倒数第2行
  （日线可能已含当日收盘）；输出 ignite5.json + 点火报告。CLI `ignite5`。
- `mainrise/m5optimize.py`：月度买卖点优化（网格搜索 vol_mult/min_chg 阈值，
  评估点火价 vs 收盘价差 + 后 1/3/5 日收益）；CLI `m5optimize`。
- 验证：中瓷电子 08-14 点火检出（10:05 123.59，比收盘 130.08 便宜 5.0%）；
  服务器点火检测 8 信号/10 追踪。

**接入**：
- daily_update.sh 17:30 后加 `m5data`（归档 110 只 ≈90s）+ `ignite5 --report`。
- 盘中 cron：09:35-14:55 每 10 分钟 ignite5（工作日）。
- 月度 cron：每月最后一天 18:10 m5optimize。
- 测试：tests/test_ignite5.py 7 项（点火判定/假点火剔除/归档合并/优化安全）；
  **170 项全绿**。

**数据边界**：5 分钟归档从 2026-08-15 起逐日积累，月度优化首月仅单日数据
（信号 186 个、买价 45% 低于收盘是单日噪声），随积累越准。东财 5 分钟历史
仅近 30 天不可回测旧交易——5 分钟研究只能前瞻验证。

## 会话记忆（2026-08-15，飞书推送接入 + 5分钟系统收尾）

**飞书推送（2026-08-15）**：用户无企业微信，改用**飞书机器人**（免费不限量）。
- push.py 新增 get_feishu_webhook（环境变量 FEISHU_WEBHOOK → settings.json
  feishu_webhook → ~/.feishu_webhook，存完整 URL）+ send_feishu（文本格式）。
- send_alert 改：**飞书优先 → 企微 → Server酱**；ignite5 点火信号推送
  飞书优先、企微兜底。
- 服务器已配 /home/ubuntu/chaoduan/.feishu_webhook（600）+ /root/.feishu_webhook
  （cron 以 root 跑，需 root 也能读）；测试推送成功。
- .gitignore 加 .feishu_webhook。

**本轮收尾修复**：
- **交割单"同日买卖"bug**：simulate 期末未平仓（open=1）的 exit_date/exit 误填
  最后一天 → 中瓷电子 08-14 显示"08-14买 08-14卖"；改为 NaN（未平仓），
  门户交割表未平仓置顶显示"持仓中"。170 测试全绿已部署。
- **候选池 3 只**：非 bug——45 天内 34 个硬规则信号，热主题+90日T0≥3 双过滤后
  剩 3 只（003031/000938/603662），正常。
- **门户卡片顺序**：买卖点提醒卡（含🔥盘中点火信号区）移到最前，市场状态/
  周期/产业/涨价卡后移。

**DSH 服务器部署（进行中）**：用户要在腾讯云服务器跑 DSH（DeepSeek Harness），
手机浏览器对话。Node 22.23.2 已装（清掉旧 node12 冲突）；pnpm 9.15.9 已装；
npm 全局安装 @deepseek-ai/dsh 进行中（github 克隆慢，走 npm registry）。
计划：systemd 服务起 dsh web（3080）→ nginx 反代 + Basic Auth → 手机访问。
待配 DeepSeek API key（$DSH_HOME/.credentials.yaml）。
